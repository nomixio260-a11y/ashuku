# ashuku 開発レポート

**重複排除+類似デルタ+zstd圧縮によるデータ保存サービス — 開発と研究の総括**

日付: 2026-07-14 / リポジトリ: `nomixio260-a11y/ashuku` / 言語: Go(純Go・CGO不要)

---

## 1. エグゼクティブサマリー

ディスク容量の少ないサーバーで大量データを保持するため、REST API型のストレージ
サービス「ashuku」をゼロから開発した。重複排除・類似チャンク検出・デルタ圧縮・
zstd圧縮・チェーン再編成を組み合わせた削減パイプラインにより、実測で:

- **高冗長ログ**: 305MB → 6.8MB(**44.7倍**)
- **バックアップ100世代(合成ワークロード)**: 論理1.56GB → 物理11.3MB(**142.1倍**)
- **バックアップ200世代**: 論理3.13GB → 物理21.3MB(**150.5倍** — 削減倍率が世代数とともに伸び続ける)
- **実データの世代バックアップ**: 121MBのtarの2世代目の物理増分 **0.18%**
- **圧縮不能データ(乱数・動画等)**: 膨張ゼロ保証(raw保存へ自動フォールバック)
- 全データはSHA-256検証付きでビット単位に復元される

情報理論上「任意データの1000:1圧縮」は不可能だが(§7)、**世代保持型バックアップ
という現実のワークロードでは論理テラバイト級→物理数ギガバイト級が達成可能**
であることを、実装と実測の両面で示した。

## 2. 背景と要件

- **課題**: サーバーのディスク容量が少ない。テラ級の論理データを数ギガ級の物理で保持したい。
- **要件**(ユーザー確認済み): データ種類はログ/バックアップ/文書/メディアすべて、
  形態はREST APIサーバー、実装はGo。
- **制約の合意**: 任意データの1000:1は情報理論上不可能。削減はデータ側の冗長性
  (重複・類似・低エントロピー)を回収することで達成する。

## 3. 成果物

### サービス

```
go build -o ashuku ./cmd/ashuku
./ashuku -addr :8080 -data ./data
```

| API | 機能 |
|---|---|
| `POST /api/v1/files` | アップロード(ストリーミング、`X-File-Name`) |
| `GET /api/v1/files/{id}` | ダウンロード(ストリーミング復元) |
| `GET /api/v1/files` | 一覧 |
| `DELETE /api/v1/files/{id}` | 削除(参照カウントGC) |
| `GET /api/v1/stats` | 論理/物理サイズ・削減倍率の可視化 |
| `POST /api/v1/optimize` | chain repack(チェーン再編成)の手動実行 |

主な設定: `-compression fast|balanced|max`、`-delta-depth`(チェーン深さ、既定32)、
`-cache-mb`(チャンクキャッシュ、既定128MiB)、`-chunk-avg`(チャンクサイズ、既定1MiB)、
`-optimize-every`(自動再編成間隔、既定1h)。

### 削減パイプライン

```
アップロード
  → FastCDC 可変長チャンク分割(内容定義境界: 挿入・シフトに強い)
  → SHA-256 重複排除(同一チャンクはストア全体で1回だけ保存)
  → 類似チャンク検索(super-feature スケッチ索引)→ 複数候補から最小デルタを採用
  → デルタ圧縮(zstd辞書方式、チェーン化+深さ上限+rebase)
  → zstd 圧縮(縮まないデータは raw 保存 = 膨張ゼロ保証)
  → 参照カウントGC / LRUキャッシュ / バックグラウンド chain repack
```

### テスト・検証

- ユニット+API結合+E2E(実サーバー起動・curl・sha256照合)全パス
- 破壊的ケースの検証: 削除カスケードGC、repack中の並行読み出し、クラッシュ安全性
  (表現ファイルのコンテンツアドレス化により、メタデータは常に実在ファイルを指す)
- ベンチマークツール `cmd/ashuku-bench` 同梱(全実測は再現可能)

## 4. 実測に基づく設計判断(開発の経緯)

本プロジェクトの特徴は、**全ての設計判断をベンチマークで裏付けた**ことである。

### 4.1 世代スケーリング(バックアップ100世代・局所変更・balanced)

| 段階 | 手法 | 削減倍率 | 学び |
|---|---|---:|---|
| v1 | dedup+zstdのみ | ~3x | 境界シフトで完全一致dedupは脆い |
| v2 | +類似デルタ(ベース固定) | 26.6x | 世代ドリフトでデルタが肥大 |
| 実験 | キーフレーム方式(深さ1) | 26.6x | 再アンカーが遅くドリフト解消せず → 不採用 |
| v3 | デルタチェーン(深さ8) | 31.6x | 再アンカー(完全コピー)コストが支配的 |
| v4 | 深さ32+LRUキャッシュ | 82.7x | キャッシュが読み出しコストを吸収(771ms→30ms) |
| v5 | +rebase(完全コピー廃止) | 82.7x | 物理半減(41→19.4MiB)、200世代の劣化を緩和 |
| **v6** | **+chain repack(星形再編成)** | **142.1x** | ドリフト蓄積の根本対策。repackは5秒 |

200世代では v5 の 34.8x が repack 後 **150.5x**(物理91.9→21.3MiB、repack 18秒)
となり、**「世代数を増やすと削減倍率が下がる」飽和問題は解消**された
(100世代142.1x → 200世代150.5x と単調増加)。repack は `-optimize-every`
(既定1時間)で自動実行される。

### 4.2 現行のベンチマーク結果(要約)

| ワークロード | 削減倍率 |
|---|---:|
| バックアップ200世代(局所変更)+optimize | **150.5x** |
| バックアップ100世代(局所変更)+optimize | **142.1x** |
| バックアップ20世代(局所変更) | 36.0x(デルタ無効なら3.4x) |
| バックアップ20世代(全域変更=dedup全滅の最悪ケース) | 26.1x(デルタ無効なら1.8x) |
| ログ(反復構造) | 5.3x |
| 疑似テキスト | 3.0x |
| 乱数(圧縮不能) | 1.0x(膨張なし) |

### 4.3 定説と異なる知見

チャンクサイズスタディ(128K〜1M)では、文献の定説「小チャンクほどdedup率向上」に
反して**1MiBが最良**だった。類似デルタ圧縮が細粒度の重複を差分としてすでに回収する
ため、小チャンクの利点が消え、チャンク単位圧縮の効率低下だけが残るためである。
**デルタ圧縮を持つ設計では「大きめチャンク+類似デルタ」が正解**という、
本実装の実測から得られた知見である。

## 5. 研究成果(並行実施した3本の調査)

1. **極限データ削減の相場観**: 実運用バックアップ(EMC 1万台実測)はdedup平均10.9x×
   圧縮1.9x≈20x。デルタ圧縮の上乗せは対象チャンクを追加で2〜2.8x(Palantir ASPLOS'24)。
   商用の公称値はプライマリ2〜5x/バックアップ10〜30x。**ashukuの実測はこの相場を上回る**
   (デルタチェーン+repackの効果)。
2. **合成フル統合のアーキテクチャ**(Veeam/Data Domain/MFDedup/git pack):
   「書き込みは貪欲に、レイアウト最適化はバックグラウンド再編成で」という共通原則を
   抽出し、Phase 1(rebase)とPhase 2〜3(chain repack)を実装に反映済み。
3. **preflate(gzip/PNG可逆再圧縮)の実装可能性**: 純Go実装は世界に存在せず、
   microsoft/preflate-rs(Rust)のcgo組み込みが唯一の現実的経路(工数6〜9人週)。
   zlib産ストリームなら補正情報0.01%でビット一致復元可能。gzip主体のワークロードで
   +43%級の効果が見込めるため、ロードマップ最上位候補。

詳細な出典・数値は [RESEARCH.md](RESEARCH.md) を参照。

## 6. 「テラ→数ギガ」の達成条件(正直な試算)

実測(世代あたり物理増分)とFAST'12の実運用統計に基づく:

| シナリオ | 論理 | 物理(概算) | 削減 |
|---|---:|---:|---:|
| 20GBを日次0.5%変化で365世代 | 7.3TB | ~19GB | ~380x |
| 20GBを日次0.1%変化で365世代 | 7.3TB | ~9GB | ~810x |
| ユニークな乱数・圧縮済みメディア 1TB | 1TB | ~1TB | 1x |

**結論**: 「テラ→数ギガ」は世代保持型ワークロードで現実に達成可能であり、
ashukuはそのための機構を全て備えた。一方、一度きりのユニークデータは
どの技術でも縮まない(これは実装の限界ではなく情報理論の限界)。

## 7. 理論的制約(なぜ「何でも1000:1」は不可能か)

- **鳩の巣原理**: すべての入力を縮める可逆写像は存在しない。
- **Kolmogorov複雑性**: ランダムなnビット列がkビット縮む確率は高々2^−k。
- **再圧縮の実験的確認**: zstd圧縮済みデータの再圧縮は実測1.0015x(0.15%)。
  取れる冗長性は「圧縮済みバイト列」ではなく「元データ」側にしか残っていない。

## 8. 今後のロードマップ(優先度順)

1. **preflate-rs 統合**(gzip/PNG/PDF内部ストリームの可逆再圧縮、+43%級)— 調査済み、cgo経路
2. **chain repack のオンライン強化**(GCのcopy-forwardとの統合、virtual synthetic化)— 設計済み
3. **JPEG可逆再圧縮**(lepton_jpeg_rust、平均22%)— 画像主体の場合
4. ~~パックファイル化~~ — **実装済み**(小デルタを追記専用パックに集約、実ディスクオーバーヘッド+8〜9%。解放領域はコンパクションで回収)
5. ~~保持期限削除の領域回収~~ — **実装済み**(ゾンビ救出: 削除済み世代のチャンクがベース参照で残る問題を、子デルタの祖父への張り替えで解決。50世代中40世代削除でチャンク279→78個に収縮)
6. **コールド層のzpaq級超高圧縮**(zstd比+50%、CPU数百倍)— アーカイブ専用

## 9. 再現方法

```sh
go test ./...                                        # 全テスト
go run ./cmd/ashuku-bench                            # 全データセット×全設定
go run ./cmd/ashuku-bench -gens 100 -only 局所変更 -optimize   # 世代スケーリング+repack
go run ./cmd/ashuku-bench -input yourdata.tar        # 実データ
```
