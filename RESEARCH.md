# RESEARCH — 「テラバイトを数ギガバイトに」への研究記録

目標: ディスク容量の少ないサーバーで、テラバイト級の論理データを数ギガバイト級の
物理容量で保持する。本書は (1) 何が理論的に可能/不可能か、(2) ashuku の実装で
実測できた削減率、(3) さらに近づくためのロードマップ、を記録する研究文書である。

## 1. 理論的な前提: 「任意データの1000:1」は不可能、「実データの1000:1」は条件付きで可能

- **鳩の巣原理**: 長さ n ビットの列は 2^n 通りあるが、n−1 ビット以下の出力は
  合計 2^n − 1 通りしかない。すべての入力を縮める可逆写像は存在せず、ある入力を
  縮める圧縮器は必ず別の入力を伸ばす。
- **Kolmogorov複雑性**: ランダムな n ビット列が k ビット以上縮む確率は高々 2^−k。
  一様ランダムデータは高確率で非圧縮。
- **Shannonの情報源符号化定理**: 可逆圧縮の期待符号長は情報源エントロピー H を
  下回れない。**1000:1 は「データの 99.9% が冗長」な場合にのみ達成可能**であり、
  それはアルゴリズムの性能ではなくデータ側の性質である。

参考: [Kolmogorov Complexity primer](https://www.jeremykun.com/2012/04/21/kolmogorov-complexity-a-primer/),
[Compression and Kolmogorov complexity](https://qchu.wordpress.com/2020/09/27/compression-and-kolmogorov-complexity/)

**結論**: 追うべきは「魔法の圧縮アルゴリズム」ではなく、**実データに現実に存在する
99%超の冗長性(バックアップ世代間の重複・類似)を余さず回収する仕組み**である。
バックアップを毎日取るワークロードでは、論理データは世代数に比例して増えるが
実変化は微小なので、世代数 × 変化率次第で 100:1〜1000:1 は物理法則に反しない。

## 2. 文献調査(2026-07-12 実施)

### 2.1 CDC重複排除の実世界削減率

- 実運用バックアップ(EMC Data Domain 1万台超の実測, FAST'12 Wallace et al.):
  **dedup 平均 10.9x / 中央値 8.7x**、dedup後のローカル圧縮 **平均 1.9x**。
  合計でおおよそ **15〜20x** が実運用の代表値。
- データ種別差が大きい: ホームディレクトリ長期保存 14.0x、混合ワークステーション
  11.0x、Eメール 9.6x、DB日次フル 2.2〜5.1x。
- FastCDC (ATC'16) は Rabin CDC と同等の dedup 率でチャンキングを3〜10倍高速化。

出典: [FAST'12 Wallace et al.](https://www.usenix.org/system/files/conference/fast12/wallace2-9-12.pdf),
[FastCDC ATC'16](https://www.usenix.org/system/files/conference/atc16/atc16-paper-xia.pdf)

### 2.2 類似検出+デルタ圧縮(post-dedup delta compression)

- 手法系譜: DERD(super-feature 原型)→ Finesse(FAST'19)→ Odess(検出7.9倍高速)
  → Palantir(ASPLOS'24, 階層的 super-feature)。
- Palantir 実測(バックアップ系7データセット): デルタ圧縮ステージ単体の削減は
  **2.0〜2.8x**、デルタ対象チャンク割合は 29〜57%。エンドツーエンドで
  Finesse 比 +26.5% の圧縮率向上。スループット低下は約8%と小さい。
- 運用上の注意: ベース参照の連鎖により読み出し・GC が複雑化する。

出典: [Finesse FAST'19](https://www.usenix.org/conference/fast19/presentation/zhang),
[Palantir ASPLOS'24](https://dl.acm.org/doi/10.1145/3620665.3640353)
([PDF](https://henryhxu.github.io/share/hongming-asplos24.pdf))

### 2.3 既圧縮フォーマットの可逆再圧縮(precompression)

- **Precomp**: zlib/Deflate(PDF, PNG, ZIP, gzip)等の内部ストリームを展開し、
  ビット一致で再構成できる場合のみ強圧縮をかけ直す。Silesia 67.6MB で
  LZMA2 単体比 **+43%** の削減例。
- **preflate-rs**(Microsoft, Rust): Deflate を「非圧縮データ+数バイトの再構成情報」
  に分解。実装部品として最有望。
- **Lepton**(Dropbox): JPEG を**平均22%**可逆削減。160億ファイル・数PBの実運用実績。

出典: [precomp-cpp](https://github.com/schnaader/precomp-cpp),
[preflate-rs](https://github.com/microsoft/preflate-rs),
[Lepton](https://dropbox.tech/infrastructure/lepton-image-compression-saving-22-losslessly-from-images-at-15mbs)

### 2.4 極限圧縮アルゴリズム(enwik9 = 1GB 英語Wikipedia, LTCB)

| 圧縮器 | 圧縮率 | コスト |
|---|---|---|
| nncp(NN) | ~9.3x | GPU前提・極低速 |
| cmix | ~9.2x | RAM 31GB・1GBに約1週間 |
| zpaq(最大) | ~7.0x | RAM 14GB・zstdの数百倍のCPU |
| zstd 高レベル | ~4.6x | 実用速度 |

**zstd 高レベルと cmix の差は約2倍、速度差は4〜5桁**。サービス用途では
zstd 高レベル+長距離マッチング+辞書がパレート最適。zpaq級はコールド
アーカイブ層専用と割り切るべき。

出典: [Large Text Compression Benchmark](https://mattmahoney.net/dc/text.html)

### 2.5 商用ストレージの公称値(期待値の相場)

- Pure Storage: 平均 **5:1**(dedup+圧縮)、総合効率 10:1
- Dell EMC Data Domain: 公称「最大50:1」、実際は 2:1〜50:1(世代保持条件に依存)
- Red Hat VDO: VM/コンテナ 10:1、汎用 3:1 が推奨プロビジョニング比
- 業界相場: **プライマリ 2〜5x、バックアップ(世代保持)10〜30x** が現実的範囲

出典: [Pure Storage blog](https://blog.purestorage.com/purely-technical/understanding-deduplication-ratios/),
[Red Hat VDO docs](https://docs.redhat.com/en/documentation/red_hat_enterprise_linux/8/html/deduplicating_and_compressing_storage/deploying-vdo_deduplicating-and-compressing-storage)

## 3. ashuku の実装と実測

### 3.1 実装した削減パイプライン

1. **FastCDC 重複排除**(平均チャンク 1MiB、`-chunk-avg` で調整可)
2. **類似チャンク検出**: gear ローリングハッシュのサンプル値に線形変換をかけた
   最大値を特徴(super-feature ×3)とし、bbolt 索引で候補検索(Odess/Finesse 系)
3. **デルタ圧縮**: 類似ベースを zstd 辞書として差分のみ保存。候補は複数評価して
   最小デルタを採用(best-of-N)。デルタチェーン(深さ上限 `-delta-depth`,
   デフォルト32)で直近世代との差分を維持し、深さ上限到達時は完全コピーではなく
   浅い祖先への張り替え(rebase)でデルタを継続
4. **zstd 圧縮**(fast/balanced/max)+ **raw フォールバック**(圧縮不能データの無膨張保証)
5. **参照カウントGC**(デルタのベース参照はカスケード解放)
6. **伸長済みチャンクの LRU キャッシュ**(デフォルト128MiB, `-cache-mb`)が
   チェーン読み出しコストを吸収(実測: 深さ32チェーンの最終世代8MiB読み出しが
   キャッシュなし771ms → あり30ms、キャッシュ命中時2ms)
7. **パックファイル**: 128KiB未満の表現は追記専用パックに集約。デルタ圧縮が効く
   ほど表現は数百B〜数KBに縮み、1表現=1ファイルでは4KiBブロック粒度で実ディスクが
   数倍に膨らむ(実測: 2KBファイル100個で2.1倍)ため。解放領域は live 率50%未満の
   パックを copy-forward するコンパクション(Optimize 内)で回収(Data Domain の
   container cleaning と同方式)。実測: 100世代バックアップで実ディスク = 論理物理
   +8%(12.2MiB vs 11.3MiB)、200世代で +9%(23.3MiB vs 21.3MiB、いずれもメタDB込み)。
8. **ゾンビ救出(保持期限削除の領域回収)**: 順方向デルタチェーンでは古い世代を
   削除しても、新しい世代のデルタがベース参照で古いチャンクを「ゾンビ」として
   生かし続ける(復元に必要なため)。Optimize がゾンビ(参照の全てが子デルタからの
   もの)を検出し、子を祖父へ張り替えてチェーンを1リンクずつ収縮させる。
   「子デルタの増分合計 < ゾンビの保存サイズ」の場合のみ実行するため物理容量は
   純減が保証される。実測: 50世代中古い40世代を削除 → チャンク 279→78 個に収縮、
   物理は残存10世代をゼロから入れ直した場合の理論下限近くまで回収。
9. **gzip precompression(zlibパラメータ探索方式)**: preflate-rs の cgo 統合は
   外部コードビルドの権限制約で見送り、その前身にあたる precomp/AntiZ 方式を
   自前実装した(§2.3)。gzip を展開し、システム zlib(cgo)で全レベルを再圧縮して
   ビット一致するレベルを探索。一致した場合のみ「展開データ+レシピ(ヘッダ原文+
   レベル)」に分解して保存し、展開データを dedup/デルタ/zstd の対象にする。
   カバレッジは zlib 産 gzip(Python/Java/nginx/PNG 等 = 実世界の最大勢力)で、
   GNU gzip / zopfli / Go 産は安全に素通し。実測: Python(zlib)産 gzip をレベル9と
   正しく認識しビット一致復元、「同内容の gzip 2世代」で2世代目の物理増分が
   展開データの 0.2%(gzip のままなら丸ごと別バイト列で dedup 全滅)。
   復元はシステム zlib の出力安定性に依存するため、保存時検証+復元時 SHA-256
   検証で「壊れたデータを返さない」ことを保証(検証失敗時はエラー)。

### 3.2 ベンチマーク結果(`go run ./cmd/ashuku-bench`, 合成データセット)

測定条件: 各データセット32MiB(バックアップは16MiB×20世代=320MiB)、
デフォルト設定(デルタチェーン深さ32・キャッシュ・best-of-N・rebase)。

| データセット | 設定 | 論理サイズ | 物理サイズ | 削減倍率 | スループット |
|---|---|---:|---:|---:|---:|
| ログ(反復構造) | fast | 32.0 MiB | 6.3 MiB | **5.1x** | 54 MB/s |
| ログ(反復構造) | balanced | 32.0 MiB | 6.1 MiB | **5.3x** | 33 MB/s |
| ログ(反復構造) | max | 32.0 MiB | 5.9 MiB | **5.5x** | 12 MB/s |
| 疑似テキスト | balanced | 32.0 MiB | 10.7 MiB | **3.0x** | 22 MB/s |
| 疑似テキスト | max | 32.0 MiB | 10.2 MiB | **3.1x** | 13 MB/s |
| 乱数(圧縮不能) | balanced | 32.0 MiB | 32.0 MiB | **1.0x** | 96 MB/s |
| バックアップ20世代(全域変更) | balanced | 320.0 MiB | 12.3 MiB | **26.1x** | 20 MB/s |
| バックアップ20世代(全域変更) | max | 320.0 MiB | 12.0 MiB | **26.7x** | 8 MB/s |
| バックアップ20世代(全域変更) | balanced+delta無効 | 320.0 MiB | 177.4 MiB | **1.8x** | 39 MB/s |
| バックアップ20世代(局所変更) | balanced | 320.0 MiB | 8.9 MiB | **36.0x** | 38 MB/s |
| バックアップ20世代(局所変更) | max | 320.0 MiB | 8.7 MiB | **36.8x** | 15 MB/s |
| バックアップ20世代(局所変更) | balanced+delta無効 | 320.0 MiB | 93.4 MiB | **3.4x** | 69 MB/s |

(「全域変更」= 毎世代0.1%の編集を全体に散らす=完全一致dedupが全滅する最悪ケース。
「局所変更」= 編集を少数ブロックに集中=現実の日次バックアップに近いケース)

ポイント:
- **デルタ圧縮の効果**: バックアップ世代ワークロードで delta 無効比 数倍〜10倍の改善。
  完全一致 dedup が全滅する「全チャンクに編集が散る」最悪ケースでも類似検出が救う。
- **raw フォールバック**: 乱数(圧縮不能)データで物理≒論理を保証(膨張なし)。
- **fast レベルはデルタ効率が悪い**: デルタ圧縮の質が下がり閾値で不採用が増える。
  デルタを活かすなら balanced 以上を推奨(デフォルト)。

### 3.3 世代数スケーリング(「テラ→数ギガ」の核心)

世代数を増やしたときの削減倍率(局所変更・balanced)。開発の各段階での改善を併記:

| 世代数 | 論理サイズ | 初期実装(深さ8・完全コピー再アンカー) | 深さ16 | **現行(深さ32+rebase+キャッシュ)** |
|---:|---:|---:|---:|---:|
| 5 | 80.0 MiB | 9.2x | 9.3x | **9.3x** |
| 20 | 320.0 MiB | 31.3x | 36.0x | **36.0x** |
| 50 | 800.0 MiB | 33.8x | 50.7x | **82.1x**(物理 9.7 MiB) |
| 100 | 1.56 GiB | 26.6x | 39.0x | **82.7x**(物理 19.4 MiB) |
| 200 | 3.13 GiB | 22.1x | 34.8x※ | **34.8x**(物理 91.9 MiB) |

(※200世代の深さ16単独測定はなし。現行値を記載)

読み取れること:

- 5→20世代の物理増分はわずか0.3MiB。**初回フルが支配的で、以降の世代は
  ほぼ差分コストのみ**。現行実装では削減倍率は100世代まで世代数にほぼ
  比例して伸び、**82.7x** に達する(初期実装の3.1倍)。
- 改善の内訳: (1) 深さ上限の引き上げ(8→32)で再アンカー頻度を削減、
  (2) **rebase**(上限到達時に完全コピーせず浅い祖先へ張り替え)でアンカー残留を
  さらに削減 — 100世代の物理は 41.0MiB(深さ16)→ 19.4MiB(現行)に半減、
  (3) LRUキャッシュが深いチェーンの読み出しコストを吸収(771ms→30ms)し、
  スループットも 23→47MB/s に改善。
- 200世代では rebase 祖先からのドリフト蓄積によりデルタが徐々に肥大し
  34.8x に低下していたが、**chain repack(Optimize)の実装により解消**:

| 世代数 | repack 前 | repack 後 | repack 時間 |
|---:|---:|---:|---:|
| 100 | 82.7x(19.4 MiB) | **142.1x(11.3 MiB)** | 5秒 |
| 200 | 34.8x(91.9 MiB) | **150.5x(21.3 MiB)** | 18秒 |

  repack は星形チェーン(同一祖先を共有するドリフトしたデルタ群)を
  デルタサイズ順(≒世代順の近似)の隣接チェーンに再編成する。各チャンクは
  「新表現が現表現より小さい場合のみ」書き換えるため物理容量は単調減少で、
  識別子(内容ハッシュ)とマニフェストは不変。100→200世代で削減倍率が
  142.1x→150.5x と**単調増加に転じ、飽和問題は解消**した。
  サーバーは `-optimize-every`(既定1時間)で自動実行する。

なお、この合成ワークロードは毎世代ランダムバイトを書き込むため世代とともに
データ自体のエントロピーが増える、実際より厳しい条件である。実データ
(Goソースツリー121MBのtar)では1世代あたりの物理増分0.18%を実測しており、
変化率の低い実バックアップでは表より高い倍率になる。

設計判断の実測記録(100世代・局所変更・balanced):

| 設計 | 削減倍率 | 備考 |
|---|---:|---|
| デルタなし(dedup+zstdのみ) | ~3x | 境界シフトで dedup が部分的に無効化 |
| デルタ(ベース固定=最古世代) | 26.6x | ドリフト蓄積でデルタが世代とともに肥大 |
| キーフレーム方式(深さ1+50%閾値再アンカー) | 26.6x | 再アンカーが遅く、ドリフト解消せず |
| デルタチェーン 深さ8(完全コピー再アンカー) | 31.6x | 再アンカーコストが支配的 |
| デルタチェーン 深さ32(完全コピー再アンカー) | 82.7x | 読み出しコストが課題だった |
| **現行: 深さ32 + rebase + キャッシュ** | **82.7x** | 読み出し771ms→30ms、200世代でも34.8x |

採用: **チェーン方式・デフォルト深さ32 + rebase + LRUキャッシュ**。
キャッシュにより深いチェーンの読み出しコストが実用範囲に収まるため、
削減率最優先の設定をデフォルトにできた。

**チャンクサイズスタディ**(文献P0推奨の検証。balanced、現行実装):

| 平均チャンク | バックアップ20世代(局所変更) | ログ |
|---:|---:|---:|
| 128 KiB | 33.5x | 5.2x |
| 256 KiB | 35.0x | 5.2x |
| 512 KiB | 35.8x | 5.3x |
| **1 MiB(デフォルト)** | **36.0x** | **5.3x** |

文献では「小チャンクほど dedup 率向上」が定説だが、**本実装では類似デルタ圧縮が
細粒度の重複を差分としてすでに回収する**ため小チャンクの利点が消え、むしろ
チャンク単位圧縮の効率低下で逆転する。1MiB デフォルトの妥当性を実証
(デルタ圧縮を持つ設計では大きめのチャンク+類似デルタが正解、という
文献の定説と異なる知見)。

### 3.4 実データでの検証(開発時のE2Eテスト)

- 高冗長ログ 305MB → 物理 6.8MB(**44.7x**)
- 121MB の tar の先頭に 1KB 挿入した「2世代目」→ 物理増分 224KB(**0.18%**)
- 乱数 50MB → 物理増分ちょうど 50MB(膨張ゼロ)
- 全ダウンロードで sha256 完全一致、削除で物理容量が正確に減少

### 3.5 「テラ→数ギガ」への現実的な試算

実測とFAST'12の実運用統計に基づく試算(20GBの業務データを毎日フルバックアップ、
日次変化率 0.5%、365世代 ≈ 論理 7.3TB):

- 世代間はデルタ/dedupでほぼ変化分のみ: 365 × 20GB × 0.5% ÷ 3(圧縮) ≈ 12GB
- 初回フル: 20GB ÷ 3 ≈ 6.7GB
- **物理合計 ≈ 19GB(論理 7.3TB → 削減率 ~380x)**

変化率 0.1% なら物理 ≈ 9GB(**~810x**)。つまり**「テラ級の論理データを数〜数十ギガ
の物理で保持」は、世代保持型ワークロードで現実に達成可能**であり、ashuku の
実装はそのための機構(dedup+類似デルタ+チェーン)を備えた。一方、**一度きりの
ユニークデータ(乱数・既圧縮メディア)のテラバイトはどの技術でも縮まない**。
これは実装の限界ではなく情報理論の限界である。

## 3.6 追加研究(2026-07-14): デルタチェーン管理と可逆再圧縮

### 合成フル統合(synthetic full consolidation)の調査結果

商用・学術の先行事例を調査した結論:

- **Veeam synthetic full**: 合成フルは 2x I/O(read+write)で、ReFS/XFS の
  ブロッククローンならメタデータ操作のみになる。逆増分(reverse incremental)は
  書き込みパス上で 3x I/O になるため現在はレガシー扱い。
- **Data Domain virtual synthetics**: dedupストレージ上の「フル」は
  レシピ(参照リスト)にすぎず、物理コピー不要でサーバ側合成できる。
- **MFDedup (FAST'21)**: 書き込みは隣接世代のみと重複検出し、レイアウト最適化は
  オフライン再編成(AVAR)に委ねる。dedup率 1.12〜2.19x 向上・復元 2.63〜11.64x
  向上・GCほぼゼロコスト。
- **git pack**: デフォルト depth 50 / window 10。「大→小へデルタ」ヒューリスティクスで
  実質逆向きデルタになり、最新版アクセスの局所性を確保。repack は既存デルタを再利用。

**共通原則**: 書き込みパスでは局所的・貪欲に。レイアウト最適化はバックグラウンドの
再編成に委ね、GC の copy-forward と統合する。

**即時反映した改善(git流 Phase 1)**: 深さ上限到達時に完全コピー(再アンカー)する
代わりに、**チェーンの浅い祖先へベースを張り替える(rebase)**方式を実装した。
書き込み増幅は新デルタ1個分のみで、長期世代保持の削減率頭打ちの主因
(アンカー残留)を除去する。深さ32チェーンの読み出しコストは伸長済みチャンクの
LRUキャッシュ(実測: 最終世代8MiBの読み出し 771ms→30ms)が吸収する。

将来の Phase 2〜4(オフライン統合ツール → shadow-write+原子的コミットによる
オンライン統合 → virtual synthetic 化)の設計詳細は調査ログ参照。

### preflate(Deflate系可逆再圧縮)のGo組み込み調査結果

- **純Goのpreflate実装・ポートは存在しない**。Go標準 `compress/flate` はzlibと
  異なるビットストリームを出すため「再圧縮一致検証」方式のカバレッジはニッチ
  (実世界のgzip生成源の最大勢力はzlib系)。
- 事実上の後継実装は **microsoft/preflate-rs**(Rust, Apache 2.0, 実運用実績あり)。
  zlib産ストリームは補正情報 約0.01% でビット一致復元。zlib-ng/libdeflate/miniz も
  認識し、未認識エンコーダ産でも(補正列は増えるが)必ず可逆。
- C FFI(チャンク駆動API)を備えるため **Rust staticlib + cgo が推奨経路**。
  JPEG可逆再圧縮(lepton_jpeg_rust, 平均22%削減)も同型のFFIで追加可能。
- 工数見積もり: deflate対応 約6〜9人週、JPEG追加 +2〜3人週。
  シングルバイナリ(純Go)の利点を失うため、導入判断はワークロード実測
  (gzip/PNG含有率)を先に行うべき(Phase 0: `preflate_util --verify` でコーパス測定)。

## 4. ロードマップ(削減率をさらに上げる)

文献調査の優先度リストに基づく:

1. **[P0] チャンクサイズ最適化** — 実装済み(`-chunk-avg`)。小チャンク(64〜256KB)は
   dedup 率を上げるがメタデータ増。ワークロード別の推奨値をベンチで確立する。
2. **[P1] 圧縮リージョン集約** — 小チャンク運用時、ユニークチャンクを ~128KB
   リージョンに集めて一括圧縮(Data Domain 方式)すると小チャンクの圧縮率劣化を
   相殺できる。
3. **[P1] zstd 辞書学習** — データ種別ごとに学習した辞書で小チャンクの圧縮率を改善。
4. **[P2→一部実装済み] Deflate 系 precompression** — zlib パラメータ探索方式
   (precomp/AntiZ 流)を実装済み(§3.1 項目9)。zlib 産 gzip をカバーする。
   残る拡張: (a) 生 zlib ストリーム(0x78 マジック、主対象 git loose object)+
   マルチメンバー gzip(logrotate 連結 / BGZF)対応 — 既存の zlib パラメータ探索を流用でき
   低コスト、設計と DoS 対策は §4.1 参照、
   (b) コンテナ内部ストリーム(PNG IDAT / PDF FlateDecode / JAR)は署名ベース個別パーサが必要、
   (c) preflate-rs 統合による zlib-ng / libdeflate / 未認識エンコーダ対応
   (外部コードのビルド許可が必要、工数 6〜9人週)。
5. **[P2] JPEG 可逆再圧縮(lepton_jpeg_rust, 平均22%)** — 画像が支配的な場合のみ。
   preflate-rs と同型の FFI で追加可能(+2〜3人週)。
6. **[P3] コールド層の超高圧縮(zpaq級)** — アクセス頻度極小の層を分離できる場合のみ。
   CPU数百倍で +50% 程度なので優先度は低い。
7. **[P1→Phase 1 実装済み] デルタチェーンの rebase と合成フル統合** — 深さ上限
   到達時の完全コピーを廃止し浅い祖先への張り替えに変更(§3.6)。残る Phase 2〜4
   (オフライン chain repack ツール、shadow-write+原子的コミットによるオンライン統合、
   virtual synthetic 化)は §3.6 の調査済み設計に従う。
8. **[運用] 期待値の可視化** — `/api/v1/stats` の削減率をワークロード判断に使う。
   削減率はアルゴリズムではなくデータで決まるため、公称値は必ず前提条件付きで示す。

## 4.1 追加研究(2026-07-15): precompression の生 zlib / マルチメンバー gzip 拡張

背景: 当実装は「zlib 産 gzip を展開データ+レシピに分解し、ビット一致検証付きで保存する」
precompression(zlib パラメータ探索方式、precomp/AntiZ 流)を実装済み(§3.1 項目9)。
本節は拡張候補 (a) 生 zlib ストリーム(0x78 マジック)、(b) マルチメンバー gzip
(連結 gzip)の安全性・実用性を Web 調査した記録である。

### 4.1.1 zlib マジックの誤検出率と対策

- **zlib ヘッダ構造(RFC 1950)**: 先頭 2 バイトは CMF(Compression Method and Flags)と
  FLG。CMF の下位 4 ビットが圧縮法(8=deflate)、上位 4 ビットが LZ77 窓サイズ(CINFO)。
  FLG は 5 ビットのチェックサム・プリセット辞書ビット(FDICT)・2 ビットの圧縮レベル。
  **検査式は (CMF*256 + FLG) mod 31 == 0**(FLG の下位 5 ビットが CMF/FLG の残りを
  31 の倍数に合わせる巡回チェック)。実用上よく見る先頭は **0x78 0x01 / 0x78 0x9C /
  0x78 0xDA**(いずれも 32KB 窓 = CINFO 7)。
  出典: [RFC 1950](https://www.rfc-editor.org/rfc/rfc1950),
  [SANS ISC: Recognizing ZLIB Compression](https://isc.sans.edu/diary/25182),
  [zlib header 解説](https://www.codestudy.net/blog/what-does-a-zlib-header-look-like/)

- **誤検出確率(先頭 2 バイトのみ・任意オフセットなしの場合)**:
  当方は先頭バイトを **0x78 固定**で判定する。CMF=0x78 のとき
  30720 mod 31 = 30 なので FLG ≡ 1 (mod 31)、すなわち FLG ∈ {1,32,63,94,125,156,187,218,249}
  の **9 値のみ**が有効。よってランダム 2 バイトが「0x78 + 有効 FLG」になる確率は
  (1/256)×(9/256) ≈ **1.37×10⁻⁴(約 1/7300)**。全 CINFO(0x08〜0x78 の 8 種)を許すと
  約 8 倍(≈1/900)。**2 バイトの一致だけでは高頻度に誤検出する**——precomp の brute/intense
  モードが不安定になる根本原因はこれ。
  出典: [RFC 1950](https://www.rfc-editor.org/rfc/rfc1950)(mod 31 規定),
  上記 FLG 値の算術は本式から導出。

- **実プロジェクトの誤検出対策**: precomp-cpp は「zLib ヘッダは 2 バイトしかないので
  誤検出ストリームが多発し、遅く・不安定になる」と明記し、対策として
  **(1) 候補の先頭数バイトを実際に inflate してみて非 zlib を弾く**、
  **(2) 最小サイズ閾値 `-s`(既定 4 バイト)**、**(3) 誤検出位置を `-i` で除外**、
  **(4) 展開→再圧縮でビット一致するストリームのみ採用**、を使う。intense モードでは
  「誤検出が先頭に来ると後続の真ストリームを隠す」危険が文書化されている。
  出典: [precomp-cpp readme](https://github.com/schnaader/precomp-cpp/blob/master/readme.txt),
  [precomp-cpp issue #93 (Misdetection of streams)](https://github.com/schnaader/precomp-cpp/issues/93),
  [encode.su precomp スレッド](https://encode.su/threads/3223-precomp-further-compress-already-compressed-files/page3)

  → **当方の設計(先頭マジック判定のみ・最小 64B・adler32 検証・ビット一致検証)は
  precomp の (1)(2)(4) を全て内包し、さらに厳しい**。adler32 末尾検証を通った時点で
  偶然一致確率は事実上ゼロ、ビット一致検証を最終ゲートに置くので**誤り「保存」は原理的に
  発生しない**(最悪でも「precompression を諦めて素通し」に劣化するだけ)。

### 4.1.2 生 zlib が現れる実世界フォーマットと効果見込み

- **git loose object / pack**: loose object は **生 zlib ストリーム**(gzip ラッパなし)。
  既定圧縮レベルは `core.loosecompression`→`core.compression`→**既定 1(0x78 0x01)**。
  pack 内オブジェクトは型/サイズをパック側に持ち、ペイロードは deflate/生 zlib。
  → **.git を丸ごとバックアップするワークロードでは生 zlib 対応の効果が大きい**
  (loose object は zlib 産である割合がほぼ 100%)。
  出典: [gitformat-pack(5)](https://git-scm.com/docs/gitformat-pack),
  [Git Internals - Packfiles](https://git-scm.com/book/en/v2/Git-Internals-Packfiles),
  [git-config core.loosecompression](https://git-scm.com/docs/git-config)

- **PNG IDAT**: 全 IDAT チャンクの連結が **1 本の zlib ストリーム**(method=8、窓 ≤32KB)。
  PDF **FlateDecode** ストリームも zlib(`/Filter /FlateDecode`)。いずれも zlib 産が
  支配的で、precompression の主要ターゲット。ただし PNG/PDF は**ファイル先頭が 0x78 でない**
  (PNG は `\x89PNG`、PDF は `%PDF`)ため、これらの内部ストリームを拾うには**コンテナ構造の
  パース(チャンク/オブジェクト境界の解釈)が必要**で、先頭マジック判定だけでは拾えない。
  出典: [PNG Compression spec](http://www.libpng.org/pub/png/spec/1.2/PNG-Compression.html),
  [FlateDecode 解説](https://pdflyst.com/glossary-of-pdf-terms/flate-compression)

- **Java JAR / class / SQLite**: JAR は ZIP(各エントリが raw deflate、zlib ラッパなし)。
  class ファイルは非圧縮。SQLite 本体は非圧縮(拡張で zlib を使う場合あり)。
  → **これらは「生 zlib(0x78)先頭判定」では拾えない**。JAR を狙うなら ZIP local file
  header のパースが必要。当面の生 zlib 拡張の主対象は **git loose object** と割り切るのが妥当。

### 4.1.3 マルチメンバー gzip の出現頻度とメンバー数

- **連結 gzip は仕様準拠**(RFC 1952: gzip ファイルは複数メンバーの連結でよい)。実世界での出所:
  - **pigz / 並列 gzip**: 入力を 128KB チャンクに分割し部分 raw deflate を連結。出力は
    単一メンバーだが、`--rsyncable` で入力依存のブロック境界を挿入。
  - **ログローテーション**: logrotate + gzip/pigz。追記のたびに個別 gzip して連結される
    運用があり、その場合マルチメンバーになる。
  - **BGZF(genomics: BAM/VCF.gz)**: **各ブロックが独立した完全準拠 gzip メンバー**で、
    圧縮前・圧縮後とも **最大 64KiB**。数 GB の BAM は**数万〜数十万メンバー**に達する。
    末尾に 28 バイトの空 BGZF ブロック(EOF マーカ)。
  出典: [RFC 1952](https://www.ietf.org/rfc/rfc1952.txt),
  [pigz(1)](https://linux.die.net/man/1/pigz),
  [BGZF (Wikipedia)](https://en.wikipedia.org/wiki/BGZF),
  [SAM/BAM spec (hts-specs)](https://samtools.github.io/hts-specs/SAMv1.pdf)

- **メンバー数が極端に多いケースの扱い**: BGZF のように**メンバーあたり ≤64KB × 数十万個**だと、
  レシピに全メンバーのヘッダ原文+zlib パラメータを保存するとメタデータが肥大化する。対策案:
  - メンバー単位でなく**「同一パラメータの連続メンバーを RLE 的にまとめる」**(BGZF は
    全ブロックが同一 zlib 設定で生成されるため、1 レシピを全メンバーに適用できることが多い)。
  - **メンバー数の上限**を設け、超過時は precompression を諦めて素通し(安全側)。
  - EOF マーカ(既知 28 バイト)は定数として特別扱いし探索対象外にする。

### 4.1.4 gzip の FTEXT/FHCRC/OS/XFL バリエーション

- **OS バイト**: 0=FAT, 3=Unix, 7=Macintosh, 11=NTFS, **255=unknown** など(RFC 1952)。
  近年は「再現性のため 255 固定」が増加(例: Python 3.11+ は `mtime=0` 指定時に OS=255)。
- **XFL**: deflate では 2=最大圧縮/最遅、4=最速。**不明・非該当なら 0**。デコーダは XFL 値を
  無視して受理する義務がある。
- **FLG のオプションビット**(FTEXT/FHCRC/FEXTRA/FNAME/FCOMMENT)や MTIME も実装ごとに多様。
  BGZF は FEXTRA(`BC` サブフィールド)を必ず持つ。
  出典: [RFC 1952](https://www.ietf.org/rfc/rfc1952.txt),
  [gzip (Wikipedia)](https://en.wikipedia.org/wiki/Gzip),
  [CPython issue #112346 (OS byte)](https://github.com/python/cpython/issues/112346)

  → **当方の設計はレシピにヘッダ原文(MTIME/OS/XFL/FLG/FEXTRA/FNAME 等)をバイト列として
  そのまま保存する方式なので、これらのバリエーションは一切問題にならない**。ヘッダを解釈して
  再構成するのではなく原文を退避するため、未知のフィールド値・ベンダ独自拡張・FEXTRA の
  中身にも無条件で可逆。deflate 本体のみを展開→パラメータ探索の対象にすればよい。

### 4.1.5 precomp のストリーム開始位置探索戦略と CPU コスト

- precomp-cpp は**任意オフセット探索**を行う。既知コンテナ(PDF/PNG/ZIP/gzip)は署名ベースで
  内部ストリーム位置を特定するが、**intense モードは「生 zlib ヘッダ」をファイル全体で走査**、
  **brute モードは「ヘッダなし zlib があらゆる位置に在り得る」と仮定して全オフセットを試す**。
  brute は **10KB のファイルでも 1 分超**かかると明記され、任意オフセット探索の CPU コストが
  実用上の最大のボトルネックであることが示されている。
  出典: [precomp-cpp readme](https://github.com/schnaader/precomp-cpp/blob/master/readme.txt),
  [schnaader.info/precomp](http://schnaader.info/precomp.php)

  → **当方の「先頭マジック判定のみ・任意オフセット探索なし」は、この CPU コストと
  誤検出不安定性を構造的に回避する正しい選択**。チャンク先頭が 0x78 かつ有効 FLG の場合だけ
  1 回試行するので、候補判定は O(1)/チャンク。git loose object(ファイル=チャンク先頭が
  生 zlib)には完全に噛み合う。PNG/PDF/JAR の**内部**ストリームは拾えないが、それは
  「任意オフセット探索を持たない」ことの必然的トレードオフであり、拾いたければ
  **コンテナ個別パーサ(署名ベースの安価な位置特定)を足す**のが precomp と同じ正攻法。

### 4.1.6 当方設計への総合評価と追加対策の提案

**評価: 設計は安全側に正しく倒れており、precomp の既知の不安定要因(2 バイト誤検出・
任意オフセット探索の CPU 爆発)を最初から避けている。** ビット一致検証が最終ゲートなので、
誤検出があっても「誤ってデータを壊す」ことは起こらず、最悪ケースは「圧縮機会の取りこぼし」。

**追加対策の提案:**

1. **誤検出(CPU 空振り)対策**: 先頭マジック一致後、**adler32 末尾検証の前に「先頭 512B〜1KB
   だけ inflate してエラーなら即棄却」**(precomp と同じ早期棄却)。1/7300 の偶然一致でも
   ほとんどはここで数百バイト inflate しただけで落ちる。
2. **zip bomb / DoS 対策(最重要)**: precompression は**必ず展開を伴う**ため展開爆弾が正面の脅威。
   deflate の理論上限は約 **1032:1**、悪意ある多重コンテナ入りでは **28,000,000:1** の報告。
   対策は業界標準の**多層防御**:
   - **ストリーミング展開+出力バイトカウンタで上限到達時に即中断**(全展開しない)。
   - **絶対上限**(例: 展開後 ≤ チャンク上限の数倍、あるいは固定 N MB)と
     **圧縮比上限**(例: 出力/入力 > 1000 で中断)の二段。
   - precompression は「展開データを保存し直す」性質上、**展開後サイズがバックアップ元の
     実サイズを超えることは通常ない**ので、上限は元チャンク長×小定数で十分。
   - 展開はサンドボックス化された goroutine で行い、**メモリ・時間クォータ**を課す。
   出典: [David Fifield "A better zip bomb" (USENIX WOOT'19)](https://www.usenix.org/system/files/woot19-paper_fifield_0.pdf),
   [zip bomb defenses (file-type GHSA-j47w-4g3g-c36v)](https://github.com/sindresorhus/file-type/security/advisories/GHSA-j47w-4g3g-c36v)
3. **マルチメンバー gzip のメタデータ肥大対策**: §4.1.3 の通り、連続同一パラメータメンバーの
   RLE 集約とメンバー数上限を実装。BGZF は全メンバー同一設定なので 1 レシピで畳める。
4. **段階導入の順序**: (a) 生 zlib 先頭判定 →主対象 **git loose object**、
   (b) マルチメンバー gzip(まず logrotate 連結 gzip、次に BGZF の RLE 最適化)、
   (c) 余力があればコンテナ内部ストリーム(PNG IDAT / PDF FlateDecode)を署名ベースで追加。
   (a)(b) は既存の zlib パラメータ探索を流用でき低コスト、(c) は個別パーサが要る。
5. **最小サイズ閾値 64B の妥当性**: precomp 既定 4B より保守的で、短命な偶然一致と
   「展開しても得しない極小ストリーム」を排除できる。git loose object のヘッダ+微小 blob は
   64B 未満もあり得るが、そうした極小オブジェクトは precompression の利得自体が小さいので
   閾値 64B で取りこぼしても実害はない(必要なら 16〜32B へ緩めても安全性は不変)。

## 4.2 追加研究(2026-07-15): 適応圧縮ポリシーの実運用事例と auto モード

「auto」モード(fastで探査→縮むチャンクのみ最高レベルで再圧縮)の設計検証として
実運用事例を調査した。要点:

- **ZFS zstd early-abort(OpenZFS 2.2)**: 本実装と同型の2段階方式
  (LZ4/zstd-1でプリテスト→失敗なら高レベルをスキップ)。実測で圧縮不能データの
  スループット **4.5倍改善、容量コスト0.3%未満**。128KB未満のブロックは
  プローブ対象外(判定コスト比率が高いため)→ 本実装も踏襲した。
  出典: [openzfs/zfs#13244](https://github.com/openzfs/zfs/pull/13244)
- **btrfs**: エントロピー推定ヒューリスティック(kernel 4.15+)。エントロピー単独では
  LZ反復を捉えられず精度に限界 → 実圧縮プローブ(本方式)の方が判定精度が高い。
- **Harnik et al. (FAST'13)**: プレフィックス試験圧縮 vs ヒューリスティックの定量比較。
  圧縮可能データ主体ならトライアル方式有利、圧縮不能データ主体(>20〜30%)なら
  ヒューリスティック有利。本実装は raw フォールバックがあるため誤判定しても
  膨張しない。出典: [To Zip or Not to Zip](https://www.usenix.org/system/files/conference/fast13/fast13-final38.pdf)
- **zstdレベル別の利得**(klauspost実測): 構造化テキスト(JSON/ログ)は
  fastest→best で **25〜33%** 縮み、混合バイナリは ~19%。auto の再圧縮は
  テキスト系で特に効く。klauspost の SpeedBestCompression ≈ zstd level 11
  (libzstd の 19 とは別物)である点に注意。
- **チャンクごとのレベル混在は安全**: zstd フレームは自己記述的(RFC 8878)で
  伸長にレベル情報は不要。ZFS が踏んだ「再圧縮時にレベル不明で壊れる」罠は、
  本実装では表現の書き換えが常にビット一致検証つきなので該当しない。
- **既知の限界(許容)**: 「fast では縮まないが高レベルなら縮む」病理データが実在
  する(ZFS でも確認)。判定コストとの釣り合いから ZFS 同様に許容する。

**auto モードの実測**(実装後):

| データセット | auto | balanced | max |
|---|---:|---:|---:|
| ログ(反復構造) | **5.5x** (7 MB/s) | 5.3x (17 MB/s) | 5.5x (4 MB/s) |
| 疑似テキスト | **3.1x** (9 MB/s) | 3.0x (25 MB/s) | 3.1x (11 MB/s) |
| 乱数(圧縮不能) | 1.0x (**79 MB/s**) | — | 1.0x (43 MB/s) |
| バックアップ20世代(局所変更) | **36.8x** (18 MB/s) | 36.0x (38 MB/s) | 36.8x (10 MB/s) |

**圧縮可能データでは max と同一の削減率、圧縮不能データでは fast 並みの
スループット(max の約2倍)**という設計目標どおりの結果。auto を新デフォルトとした。

## 4.3 追加研究(2026-07-16): クライアント支援プロトコル

サーバーの CPU・メモリコストを最小化するため、圧縮・展開・チャンク分割を
クライアント側に移すプロトコルを設計・実装した。設計前に restic / kopia /
borg / casync / bup / Git LFS / Bazel REAPI を調査した結果:

- **業界標準と一致**: 主要 OSS はすべて「クライアントが分割・圧縮(・暗号化)、
  サーバーはダムストレージ」。missing 問い合わせ+個別 PUT は
  Bazel REAPI の FindMissingBlobs / Git LFS Batch API と同型。
- **チャンク汚染対策(必須)**: クロスユーザー共有 dedup では「ハッシュHの
  偽データ」登録攻撃(duplicate-faking)への唯一の防衛線が**サーバー側の
  伸長+SHA-256再検証**(restic rest-server のデフォルトと同方式)。
  本実装では検証を省略するオプションを意図的に作っていない。
  検証(伸長 500MB/s 超+ハッシュ)は圧縮(10〜50MB/s)より1桁以上安い。
- **Dark Clouds 型攻撃対策**: 「ハッシュを知っている=内容を取得できる」と
  いう CAS の素朴な性質は攻撃面になる(USENIX Sec'11 で Dropbox に実証)。
  本実装は「存在確認(missing 応答)」と「読み出し権」を分離し、チャンク
  ダウンロードは**自分のマニフェストが参照しているチャンクのみ**許可する
  (ownerchunks 索引)。存在サイドチャネル(Harnik et al. 2010)自体は
  クロスユーザー dedup の本質的トレードオフとして残る(認証必須で緩和)。
- **staged チャンクの TTL GC**: kopia の SessionExpirationAge(96h)、
  S3 multipart の AbortIncompleteMultipartUpload(推奨7日)と同型の
  実績ある方式。missing 応答時に staged TTL を延長し、コミットは
  全チャンク存在確認を原子的に行い、欠損時は 409+missing 一覧で
  クライアントが冪等リトライする(GC とのレースを排除)。

**実測**(1.16GB の高冗長テキスト、同一マシン):

| 経路 | 経過時間 | サーバーCPU | サーバーRSS | 転送量 | 最終ディスク |
|---|---:|---:|---:|---:|---:|
| 従来(サーバー圧縮) | 31秒 | 25.7秒 | 442MB | 1.16GB | 10.18MB |
| **クライアント経路** | **15秒** | **7.7秒(−70%)** | **40MB(−91%)** | **9.1MB(−99.2%)** | 10.18MB |

クライアント経路のチャンクは取り込み時にデルタ判定をせず(サーバーCPU最小化)、
バックグラウンドの**オフラインデルタパス**(Optimize 内、DeltaTried フラグで
一度だけ判定)が後追いで類似デルタに圧縮し直す。圧縮率は維持したまま、
取り込みコストだけを削った。

## 4.4 追加改善(2026-07-16): 本家 libzstd level 19 と super-feature

§4.2 の調査で判明した「純Go実装の最高レベル = 本家 level 11 相当」の
ギャップを埋めるため、本家 libzstd(level 19)をエンコーダとして統合した
(開発ヘッダ不要の直接リンク、出力は標準フレームなので復号・保存形式は不変)。
あわせて類似検出を単一特徴 any-match(3特徴)から Odess/DERD 標準の
super-feature 構成(12変換→4SF)に強化した。

実測(auto モード、前回値との比較):

| データセット | 改善前 | **改善後** | 物理削減 |
|---|---:|---:|---:|
| ログ(反復構造) | 5.5x (5.9 MiB) | **6.7x (4.8 MiB)** | **−19%** |
| 疑似テキスト | 3.1x (10.2 MiB) | **3.4x (9.5 MiB)** | −7% |
| バックアップ20世代(局所変更) | 36.8x (8.7 MiB) | **37.9x (8.4 MiB)** | −3% |
| バックアップ100世代 +optimize | 142.1x | 139.1x | ±誤差(デルタ支配のため) |

- **単発データ(ログ・テキスト)で効果大**(zstd レベル差がそのまま効く)。
  バックアップ系はデルタ圧縮が支配的なため効果は小さい(想定どおり)。
- **トレードオフ**: level 19 は遅い(サーバー経路の取り込みが 2〜5MB/s)。
  ただし推奨構成のクライアント経路では取り込みは検証のみ(高速)で、
  level 19 への到達はバックグラウンドのオフライン再圧縮が担うため、
  ユーザー体感には影響しない。
- クライアント経路のチャンクは Optimize の再圧縮パス(`recompressed`)で
  本家 19 に収束する。

## 4.5 追加改善(2026-07-16): PNG precompression とデルタレベルの検証

- **PNG precompression**: PNG の IDAT(libpng産の zlib=フィルタ済みスキャン
  ライン)を展開し dedup/デルタ/zstd-19 の対象にする。IDAT 分割位置を
  レシピに記録し CRC32 再計算でビット一致再構成。precompression 対応は
  gzip/連結gzip/生zlib/PNG の4形式に。既圧縮メディアでも「同じ圧縮器で
  作られた類似ファイル」なら展開データ経由で dedup が効くようになる。
- **デルタエンコーダのレベル検証(A/B)**: デルタ圧縮のレベルを
  balanced→best に上げた場合を100世代バックアップで実測:

  | 設定 | 削減率(+optimize) | repack 時間 |
  |---|---:|---:|
  | delta=balanced(現行) | 139.1x | 6秒 |
  | delta=best | 139.2x | 21秒 |

  削減率の差は 0.1x(誤差)、repack は3.5倍遅い。**デルタは既に小さな差分
  なのでエンコーダレベルを上げてもほぼ縮まず、コストだけ増える**。
  現行の balanced が正解と確認し、変更しない判断をした
  (「測って、変えない」ことを決めたのも成果)。

## 4.6 追加改善(2026-07-16): リージョン(ソリッド)圧縮

これまでの実測から、**チャンク独立圧縮が失っている「チャンクをまたぐ冗長性」**が
最大の残ヘッドルームと特定した。実データ(Goソースツリー tar 126MB)での測定:

| 方式 | 削減率 |
|---|---:|
| チャンク独立 zstd-19(改善前) | 5.45x |
| 逐次デルタ(隣接チャンク辞書) | 5.50x(+0.9%、不採用) |
| **8チャンク束リージョン圧縮** | **5.92x(+8.6%)** |
| 16チャンク束 | 5.98x(+9.7%) |
| ソリッド(全体1本) | 6.01x(+10.3%) |

利得は「zstd が多数チャンクの統計を一度に見ること」から来ており、ペアワイズの
逐次デルタでは取れない。8チャンク束でソリッドの利得の8割超が取れ、読み出しは
1リージョン(~12MiB)伸長で済みキャッシュが吸収する。

実装(Data Domain の compression region 相当):オフラインパス(Optimize)で
ファイルのマニフェスト順に連続する独立圧縮チャンクを regionChunks(8)個ずつ
まとめ、生バイトを連結して1本の zstd-19 で再圧縮する。各チャンクのメタは
(RegionID, RegionOff) を指し、読み出しはリージョン伸長→スライス→SHA-256 検証。
生存率50%未満のリージョンはコンパクション(解体→詰め直し)で回収。
クラッシュ安全機構・「縮む場合のみ切替」の単調減少保証は pack/repack と同じ。

**E2E実測**: 実データ tar を投入し Optimize で 5.50x → 5.92x(**+7.6%**、
リージョン10本76チャンク)、ビット一致復元を確認。取り込み経路は変えず
(サーバーCPU最小のまま)、リージョン化はバックグラウンドで行う。

## 4.7 追加改善(2026-07-16): 信頼性・安定性

ストレージサービスとしての信頼性を大幅に強化した。

- **クラッシュ耐久性の修正(バグ修正)**: チャンク/リージョンの書き込みは
  temp+rename でアトミックだったが、**ファイル本体と親ディレクトリの fsync が
  抜けていた**。これはメタデータ(bbolt は commit で fsync)がチャンクを指すのに
  実データが page cache のまま失われる=データ損失の穴だった。durableWrite で
  (1)temp fsync →(2)rename →(3)親ディレクトリ fsync の順を保証。
  E2E で `kill -9` 後の完全復元を確認。
- **データ完全性スクラブ**: 全チャンクを内容ハッシュで照合し、bit rot・部分
  書き込み・欠損を検出。コンテンツアドレスなので検証は伸長+SHA-256照合で確実。
  破損チャンクとその影響ファイルを報告する(`-scrub-every`、`POST /scrub`)。
- **パニック分離**: HTTP の ServeHTTP とバックグラウンドジョブ(Optimize/Scrub)に
  recover を入れ、1リクエスト/1ジョブのパニックがサーバー全体を落とさない。
- **起動時の孤児 temp 掃除**: クラッシュで rename 前に残った .tmp-* を回収。

いずれもテストで検証(破損検出・欠損検出・temp掃除・パニック復帰・耐久性E2E)。

## 4.8 追加改善(2026-07-16): メタバックアップと fsck

信頼性の残る2つの穴を埋めた。

- **メタデータバックアップ(SPOF 対策)**: チャンク実データはコンテンツアドレスの
  ファイル群として分散するが、メタデータ(bbolt の meta.db)は単一ファイルで、
  これが壊れると全チャンクが無事でも全損になる唯一の単一障害点だった。bbolt の
  MVCC を使った一貫スナップショット(tx.WriteTo)を稼働中でも安全に取り、
  別ディレクトリへ世代保持する(`-meta-backup-every`、既定1h・24世代)。
  復元可能性(有効な bbolt DB としてマニフェストが読める)をテストで確認。
- **メタデータ整合性検証(fsck)**: Scrub がデータ完全性を見るのに対し、Fsck は
  メタ整合性を見る補完。全マニフェスト・デルタ・リージョンから参照カウントの
  期待値を再計算し、保存値との drift・孤児チャンク(容量リーク)・壊れた参照
  (データ欠損)・所有者使用量の不整合を検出。`repair` で参照カウント修正・
  孤児回収・使用量再計算を行う(壊れた参照はメタバックアップからの復旧が必要)。
  テストで参照カウント破壊の検出・修復、孤児回収を確認。

これで「データ(scrub)」「メタ整合性(fsck)」「メタ耐障害(backup)」の三重の
防御が揃い、ストレージサービスとしての信頼性が実運用水準に達した。

## 4.9 追加改善(2026-07-16): 本番デプロイ対応

実運用で最初に問題になる運用面を整備した。

- **ディスク枯渇対策(最大の運用事故要因)**: ストレージサービスの停止/破損の
  最頻原因はディスク満杯。空きが `-min-free`(既定1GiB)を切ったらアップロードを
  507 で事前に拒否し、`/healthz` を 503(readiness NG)にしてロードバランサに
  知らせる。syscall.Statfs で空きを取得。
- **可観測性**: 外部依存なしの自前 Prometheus メトリクス(`/metrics`)—
  リクエスト数(ステータスクラス別)・アップロード試行/失敗・転送量・パニック数の
  カウンタと、ストレージ統計・ディスク空きのゲージ。`-access-log` で
  リクエストごとのアクセスログ(method/path/status/duration)。
- **TLS**: `-tls-cert`/`-tls-key` で HTTPS 直接待ち受け(E2E確認)。
- **デプロイ成果物**: マルチステージ Dockerfile(CGOビルド+実行イメージに
  zlib/libzstd 同梱)、docker-compose、systemd ユニット(堅牢化+graceful stop)、
  APIキー設定例。

これで「動く」だけでなく「運用できる」水準に達した。テスト3件追加
(メトリクス・ディスクガード・ヘルス)、E2E で metrics/access-log/TLS を確認。

## 4.10 追加改善(2026-07-16): 小ファイル多数のワークロード対応

**問い**: 「10GB のファイル1つ」と「100MB × 100個(=10GB)」で圧縮結果は違うか。

**答え**: この2つは**ほぼ同じ**。どちらも平均1MiBチャンクで ~1万チャンクに割れ、
重複排除・圧縮の効きは変わらない(差はファイル末尾のラントチャンクが1個 vs
100個、マニフェストが1件 vs 100件という誤差のみ)。

本当に問題になるのは**「多数のユーザーが本当に小さいファイル(数KB〜数百KB)を
大量に投下する」**ケース。FastCDC の最小チャンクは平均の 1/4(既定 256KiB)なので、
それ未満のファイルは**丸ごと単一チャンク**になる。ここに2つの弱点があった:

1. **ファイル横断のソリッド圧縮が効かない(最大の圧縮ロス)**: 既存のリージョン
   圧縮(§4.6)は1ファイル内の連続チャンク(2個以上)しかまとめない。単一チャンクの
   小ファイルは対象外で、各ファイルが**独立に** zstd 圧縮される。小さなデータは
   zstd の辞書が温まらず圧縮率が落ち、かつ「同種の小ファイル群が共有する語彙・
   スキーマ」(同じJSONキー、同じログ書式、同じHTML雛形)という**ファイルを
   またぐ冗長性**を丸ごと取りこぼす。近似重複ではないので取り込み時のデルタでも
   拾えない。
2. **ブロック浪費**: 小さな保存表現は既にパックに集約済み(§pack)なので、ここは
   対策済み。

**対策**: `buildSmallChunkRegions`(Optimize のオフラインパス)を追加。zstd 圧縮済みで
`smallChunkMax`(512KiB)以下の独立チャンクをファイル横断で集め、スーパー
フィーチャで内容の近いものを隣接させてから、生バイトで `smallRegionRawMax`(4MiB)
または64メンバーまで束ねて1本の zstd-19 でソリッド圧縮する。§4.6 の
「連結後が縮む場合のみ採用」の単調減少保証をそのまま使うので後退しない。
採用されなかった小チャンクは `RegionTried` を立て、Optimize のたびに同じ束を
書き捨て再試行する空回りを防ぐ(対象は新しく届いた小チャンクだけになる)。
対象を zstd 済みに限るのは、raw(圧縮不能=乱数・既圧縮メディア)を束ねても
効かず書き捨てを増やすだけだから。

**実測**: 共有語彙を持つが互いに近似重複ではない 40KiB の小ファイル 200個
(計3.6MB)を投入し Optimize:

| | 物理サイズ |
|---|---:|
| 改善前(各ファイル独立 zstd) | 3.63 MB |
| **小チャンクソリッド圧縮後** | **1.91 MB(-47.4%)** |

リージョン4本に200チャンクを収容、全200ファイルをビット一致で復元。2回目の
Optimize は追加リージョン0本(冪等、RegionTried で空回りなし)。読み出しは
1リージョン(最大4MiB)伸長でキャッシュが吸収する。取り込み経路は不変
(サーバーCPU最小のまま、束ね直しはバックグラウンド)。

なお極小ファイル(数KB)ではメタデータ(マニフェスト+チャンクメタ)の固定費が
相対的に効いてくるが、これはペイロードに対する情報量として不可避で、パック集約と
ソリッド圧縮で実データの物理は最小化される。

## 4.11 追加改善(2026-07-16): 多ユーザー時の一覧取得スケーラビリティ

小ファイル多数の運用では、**多数のユーザーがそれぞれ多数のファイルを持つ**。
ここで `List(owner)`(`GET /api/v1/files`)が全ファイルバケットを走査して所有者で
絞る O(総ファイル数)実装だと、サービスが成長するほど**全ユーザーの一覧取得が
店全体の規模に比例して遅くなる**という時限爆弾になっていた(100万ファイルなら
1回の一覧で100万件の JSON アンマーシャル)。

**対策**: 所有者→ファイルの二次索引 `fileowners`(キー = `ownerKey(owner) + 0x1F +
fileID`、値は空)を追加。`List(owner)` はこの索引をプレフィックス走査し、その
所有者のファイルだけを引く(**O(その所有者のファイル数)**、他ユーザーのファイル数に
非依存)。区切りの 0x1F により所有者IDが別のIDの接頭辞でも(`u` と `u2` 等)
混ざらない。索引の維持は `putFileManifest`/`deleteFileManifest` に集約したので、
サーバー取り込み経路・クライアント支援経路の双方が自動で一貫を保つ。

- **移行**: 索引導入前のストアは Open 時に一度だけ全ファイルから索引を再構築
  (フラグで二度目以降はスキップ)。
- **整合性**: fsck が索引の孤児エントリ(実在しないファイルを指す)・欠損エントリ
  (実在ファイルに無い)を検出し、repair で修復する。

Stats など「全ファイルが必要」な集計は従来どおり全走査(正しい)。所有者スコープの
操作だけを索引化した。テスト5件追加(所有者分離・接頭辞衝突・削除時維持・
移行バックフィル・fsck 修復)。

## 4.12 総点検(2026-07-17): 妥協なしの全面改善

コードベース全体を再監査し、見つかった問題を全て潰した。

### (a) パック追記の耐久性バグ修正(重大)

チャンクファイル・リージョンファイルは durableWrite(fsync + rename + 親dir
fsync)で守られていたが、**パックファイルへの追記は 4MiB ごとにしか fsync して
いなかった**。bbolt のメタコミット(fsync 済み)がパック内の位置を指した直後に
クラッシュすると、page cache 上の追記が失われ「メタが指すのに実体が無い」
データ損失になる(scrub で検出はできるが復旧不能)。

修正: **同期水位方式**。追記自体は fsync せず、メタが位置を指す直前に
`ensureSynced(pack, end)` を呼ぶ。1回の fsync がその時点までの全追記を
カバーするため、並行する多数の小さな追記の fsync は自然に1回へ合流する
(kopia が blob flush → index write で守るのと同じ順序保証)。パックの
ロール時にはディレクトリエントリも fsync し、Sync/Close のエラーも伝播させる。

### (b) 書き込みスループット: 適応グループコミット

実測(16KiB 小ファイル、fast 圧縮): 順次 9.85ms/件、**並行でも 7.75ms/件**。
bbolt の単一ライタで fsync が直列化され、並行にしてもほぼ速くならないことを
確認した(多ユーザー同時アップロードの上限 ≈ 130件/s)。

2つの修正:
1. **新規チャンクの空書き込みTx除去**: 重複チェックの高速パスが、チャンクが
   存在しない場合(=新規、大半)も空の書き込みTxをコミットし fsync を1回
   浪費していた。読み取りTxで確認し、既存のときだけ書き込みTxを使う。
2. **適応グループコミット**: 書き込み要求が並行しているときだけ bbolt の
   `Batch`(複数コミットを1つのTx・1回の fsync に合流、合流窓 2ms)へ切り替え、
   単独の書き込みは従来の `Update`(追加遅延ゼロ)を使う。Batch の再実行仕様に
   合わせ、全ホットパスのTx関数を冪等化した(外部変数は関数先頭でリセット)。

実測(同一ベンチ):

| ワークロード | 改善前 | 改善後 | 倍率 |
|---|---:|---:|---:|
| 順次アップロード | 9.85 ms/件 | 9.12 ms/件 | 1.08x |
| **並行アップロード(多ユーザー)** | 7.75 ms/件 | **2.81 ms/件** | **2.8x** |
| 並行・重複データ(dedupヒット) | 4.10 ms/件 | **1.08 ms/件** | **3.8x** |

(a) の fsync 追加(耐久性強化)を含んだ上での高速化である点に注意。

### (c) 一覧索引 v2: 巨大マニフェスト非依存・ソート不要・カーソルページング

§4.11 の索引は「所有者のファイルだけ走査」までだったが、まだ (1) マニフェスト
本体(1万チャンクのファイルで ~660KB の JSON)を一覧のためだけに全パースし、
(2) 全件をメモリに集めてソートし、(3) ページングできなかった。

索引キーを `所有者 + 0x1F + 反転UnixNano(8B) + fileID` に変更(昇順走査=
新しい順、ソート消滅)、値に一覧表示レコード {id, name, size, created_at} を
内蔵(マニフェスト本体を読まない)。`ListPage(owner, after, limit)` で不透明
カーソルによるページングを提供し、API は `?limit=` / `?after=` /
`next_cursor` で公開(省略時は従来どおり全件=後方互換)。fsck は索引の
孤児・欠損に加えて**レコード内容の食い違い**も検出・修復する。旧形式ストアは
Open 時に一度だけ再構築(v2 フラグ)。

### (d) API 硬化

- **APIキーの定時間比較**: マップ直接引きをやめ、全登録キーと
  `subtle.ConstantTimeCompare` で照合(応答時間からキーを推測するタイミング
  攻撃の芽を摘む)。
- **Content-Length 事前拒否**: 上限・クォータを申告サイズが明らかに超える
  アップロードをボディを読む前に 413/507 で拒否(帯域・CPUの浪費防止。
  申告なし・虚偽はコミット時の権威検査で従来どおり防ぐ)。
- **クライアント支援経路のメトリクス欠落**: チャンクPUT/マニフェスト確定が
  転送量・アップロード数・エラー数に計上されていなかったのを修正
  (運用ダッシュボードがクライアント経路を見落とさない)。
- チャンクGET に Content-Length を付与。

### 検討して不採用にしたもの

- **チャンクハッシュのバイナリ格納**(hex 64B → 32B): メタデータ全域の
  マイグレーションを要する割に、1MiBチャンク運用ではメタは物理の 0.01% 未満で
  利得が誤差。リスクが利得を上回るため不採用(小チャンク運用が主流になったら
  再検討)。

## 4.13 追加改善(2026-07-17): 小ファイルの単一トランザクション取り込み

小ファイル1個の保存が「チャンク確定Tx + マニフェスト確定Tx」の2回の書き込み
トランザクション(= 2回の bbolt fsync)を使っていた。多数の小ファイル投下
(本ワークロードの主役)では fsync 数がそのままスループット上限になる。

**再設計**: チャンク取り込みを prepare(存在確認・圧縮・類似検索。ロック外)と
apply(メタ操作と表現書き込み。トランザクション内)に分離した。小さなファイル
(バッファ上限 = 最大チャンクサイズ 4MiB まで)はチャンクを確定せず prepare
だけ行い、**マニフェストと同一トランザクションでまとめて確定**する。
大きなファイルは従来どおりストリーミング逐次確定(メモリ一定)で、境界を
またぐファイルはバッファ分を flush してからストリーミングに切り替える。

副次効果として**原子性が完全になった**: 以前はクォータ超過時に「チャンクを
コミット→ロールバック」という一時的な中間状態があったが、今はクォータを
トランザクション先頭で判定するため、超過時はチャンクの書き込みすら始まらない
(失敗時にディスク上へ何も残らない)。「既存見込みのチャンクが確定前に消える」
稀なレースは errChunkVanished でロック外の再圧縮に戻して再試行する
(書き込みロック内で圧縮 CPU を使わない設計を維持)。

実測(16KiB 小ファイル、fast、3回測定の中央値):

| ワークロード | §4.12 後 | 単一Tx統合後 | 今日の開始時から |
|---|---:|---:|---:|
| 順次アップロード | 9.12 ms/件 | **~6.0 ms/件** | 9.85 → 6.0(**1.6x**) |
| 並行アップロード | 2.81 ms/件 | ~2.7 ms/件 | 7.75 → 2.7(**2.9x**) |
| 並行・重複データ | 1.08 ms/件 | ~0.99 ms/件 | 4.10 → 0.99(**4.1x**) |

順次(1ユーザーの大量投下)は bolt fsync が 2→1 になり directly 効く。並行は
既にグループコミットで fsync が合流しているため横ばい(Tx 数半減はロック保持
時間の短縮として効く)。テスト3件追加(クォータ原子性・ファイル内自己重複・
バッファ境界群)、全スイートで回帰なし。

## 4.14 追加改善(2026-07-17): コンテナ形式の分解(ZIP / PDF)

ロードマップ最大の未回収領域だった「既圧縮コンテナ」に対応した。ZIP
(docx/xlsx/pptx/jar/apk を含む)と PDF は deflate/zlib ストリームがメタ構造に
埋まったコンテナで、これまで「圧縮不能」としてほぼ 1.0x で素通りしていた。

**方式**(既存の zlib パラメータ探索 + ビット一致検証を拡張):
- **ZIP**: セントラルディレクトリから deflate メンバーの位置を特定し、
  zlib 産と一致再現できるメンバーだけ展開データに置換。
- **PDF**: "stream" トークン直後の zlib 候補を消費バイト追跡つき inflate +
  adler32 検証で特定し、同様に置換。
- 保存形は「スケルトン(ストリーム除去後の原文)+ 展開データ列」の連結を
  そのままチャンク化。**一致しないメンバーはスケルトンに残す部分適用**が
  可能なのが gzip 系(全或いは無)との違いで、エンコーダ不明のメンバーが
  混ざる実世界の ZIP でも一致分だけ利得が出る。
- 分解時に全体をビット一致で自己再構成検証してから採用(失敗は素通し)。
  読み出し時も OrigSHA256 で最終検証。クライアント経路は §改修済みの
  自動フォールバックで正しく元バイト列を受け取る。

**効果は2段**:
1. **再圧縮**: deflate(≒zlib-6)を zstd-19 で置き直す。テキスト系 XML で
   実測 25.8x(手組み zip、従来 ~1.0x)。実世界相当(python zipfile)でも
   1.0x → 1.15x〜(内容依存)。
2. **世代間 dedup(より大きい)**: 展開データとスケルトンがチャンク化される
   ため、「ほぼ同じ ZIP の別世代」がチャンク共有する。実測: 6メンバー中
   1つ変更した v2(388KB)の物理増分 **+70.9KB(-82%)**。従来は deflate
   ストリームのビット差のため **+388KB(共有ゼロ)** だった。バージョン
   管理された jar / docx の繰り返しアップロードに直撃する改善。

制約: 分解対象は元ストリームが precomp バッファ上限(-precomp-max、既定
64MiB)以下のファイル。zlib 産でない deflate(Go・Info-ZIP 等)は preflate
統合(§ロードマップ)までスケルトン側に残る。

## 4.15 追加研究(2026-07-17): level 22 大窓・リージョン束サイズ・zlib 探索空間

3系統を並行測定し、2つを採用・1つを不採用にした。

### (a) libzstd level 22(ultra)+ 大窓 + long-distance matching

CCtx API(ZSTD_CCtx_setParameter)を自前宣言で叩き、オフライン経路
(リージョン圧縮・背景再圧縮)を level 19 → **22 + 入力サイズに合わせた
窓(上限 64MiB)+ LDM** に引き上げた(CompressMax)。取り込み経路は
19 のまま(22 は数倍遅く、ユーザーを待たせる場所では割に合わない)。
窓上限 64MiB は klauspost 純Goデコーダの既定上限(512MiB)より十分小さく、
復号側の変更なしで安全(16MiB 入力・窓24 の往復をテストで確認)。

### (b) リージョン束サイズの再測定(実データ)

Goソースツリー tar(64MiB)を 1MiB チャンクに割り、束サイズ×エンコーダで
「独立圧縮 level19 比の物理削減」を測定:

| 束 | level 19 | level 22+大窓 |
|---|---:|---:|
| 8(旧設定) | 9.97% | 10.12% |
| **16(新設定)** | 10.98% | **11.35%** |
| 32 | 11.42% | 12.26% |

**採用: 束16 + level22**。32 はさらに +0.9pp だがコールドなランダム読みの
伸長が最大 ~32MiB になるため、利得の大半を取りつつ読み出しコストを半分に
抑える 16 を選んだ。小チャンク(小ファイル)リージョンは 4MiB 上限のまま
(小ファイルはランダムアクセスが主で、読み出し増幅に敏感)。
合成コーパス(短語彙・長距離構造なし)では 8→32 の差が +0.2% しかなく、
**この種の判断は実データでしか測れない**ことも再確認した(合成で決めて
いたら 16 への引き上げは棄却していた)。

### (c) zlib 探索空間の拡張(precompression の適用範囲拡大)

findLevel の探索を「レベル1..9 × memLevel 8 × 既定 strategy」(9候補)から
「× memLevel 9(MAX_MEM_LEVEL を指定するライブラリ)・× Z_FILTERED
(PNG 最適化ツール等)」(最大27候補、8MiB 超は13候補に縮小)へ拡張した。
これで gzip / ZIP / PDF / PNG の**分解が成立するプロデューサの範囲**が
広がる(1候補=1回の deflate なので、非 zlib ストリームの棄却コストは最大
3倍になるが、実世界の大半は 6/9 の先頭2候補で即決する)。

レシピ互換: レベルフィールドに (level | memLevel<<8 | strategy<<16) を
符号化し、0..9 は従来解釈のままにして既存レシピと後方互換。
Z_HUFFMAN_ONLY / Z_RLE をストリーム全体に使うプロデューサは実世界に
ほぼ存在しないため探索に含めない(判断を記録)。

### 検討して不採用にしたもの

- **brotli / xz(LZMA)の追加**: テキストで zstd-19 比 5〜10% 小さくなる
  ケースがあるが、チャンク表現の形式が増えるとクライアント支援プロトコル
  (クライアントが表現をそのまま伸長する)が壊れ、依存も増える。
  zstd 単一形式の単純さを優先して不採用。
- **ストア全体の zstd 辞書学習**: リージョン(ソリッド)圧縮が既にチャンク
  間冗長性を回収しており、辞書の追加利得は重複領域。辞書の配布・世代管理の
  複雑さに見合わない。小チャンクのファイル横断リージョンが実質的に
  「動的辞書」の役割を果たしている。

## 4.16 追加改善(2026-07-18): 大規模ストア対応(スケールの4点セット)

数百万チャンク規模で問題になる「フルスキャン依存」を一掃した。

1. **Stats の O(1) 化(維持カウンタ)**: 以前はチャンク全走査 O(N) で、
   大規模ストアでは1回が秒単位だった。ファイル/チャンク/リージョンの
   メタ書き込みヘルパ(putChunkMeta 等)が同一トランザクション内で
   カウンタを新旧差分で更新する(クラッシュ整合)。全ミューテーションが
   集約ヘルパ経由である構造を利用し、呼び出し側の変更ゼロで一元化。
   fsck が全走査と照合・修復する(CounterMismatches)。

2. **Optimize のインクリメンタル化**: 毎時のフルメタ走査(collectStars /
   offlineDeltaPass / buildRegions / …)は 1000万チャンクで毎回数GBの
   メタ読みになる。作業キュー(dirtychunks / dirtyfiles バケット。取り込みが
   同一Txで積む)を導入し、高頻度パス(OptimizeIncremental、毎時)は
   **O(新着データ)** で新チャンクのデルタ/再圧縮・新ファイルのリージョン化・
   小チャンク横断ソリッドだけを行う。削除起因の再編成(星形 repack・
   ゾンビ救出・staged 掃除)は低頻度のフルパス(-optimize-full-every、
   既定24h)に分離。API は /optimize?full=1。

3. **ローリングスクラブ+検証漏れの修正**: (a) 全チャンク一括走査
   (TBクラスで数時間ディスク飽和)を、進捗カーソル付きのバッチ
   (-scrub-batch、既定10万チャンク/回)に変更。数回で一周し、周回完了で
   カーソルをリセットする。(b) **バグ修正**: 従来のスクラブは readChunk
   経由でキャッシュを読んでおり、ホットなチャンクのディスク上の bit rot を
   検出できなかった(テストで実証: キャッシュに正データ・ディスク破損の
   状態で healthy を返していた)。検証読みはキャッシュを見ず・入れずに変更
   (走査がキャッシュを洗い流す問題も同時に解消)。

4. **Delete の単一トランザクション化**: マニフェスト削除と参照解放が
   2トランザクションで、間のクラッシュで参照カウントのリーク(fsck までの
   容量リーク)が起きえた。1トランザクションに統合して原子化。

検証: カウンタ整合の不変量テスト(取り込み・重複・削除・リージョン化・
repack・パック回収の全経路後に全走査と一致)、ローリング一周の網羅性、
キャッシュ背後の破損検出、インクリメンタル→フルの引き継ぎ。全スイート緑。

## 4.17 追加改善(2026-07-18): JPEG 可逆再圧縮(純Go・cgo不要)

「画像は raw 保存されるだけで縮まない」という積み残しに対応した。JPEG は
既に DCT+量子化+Huffman 済みで外側から zstd をかけてもほぼ 1.0x だが、
**Huffman を復号して量子化 DCT 係数に戻し、係数を並べ替えて zstd/dedup/
デルタの対象にする**ことで縮められる(lepton/packJPG と同じ「係数を可逆に
取り出す」原理。エントロピー再符号化に算術符号ではなく zstd を使うぶん
利得は控えめだが、外部依存ゼロ・純Goで動く)。

**実装**(`internal/precomp/jpeg.go` ほか、純Go・cgo不要):
- baseline sequential DCT(SOF0)/ Huffman / 8ビット / リスタートマーカ /
  任意サブサンプリング(4:2:0・4:4:4 等)/ 最適化Huffmanテーブルに対応。
  プログレッシブ・算術符号・12ビットは素通し(元のまま raw 保存)。
- 係数を「成分×係数位置ごとの平面」に並べ替えて zigzag varint で保存。
  高周波成分に集中するゼロがまとまり zstd の RLE/FSE が効く。DC は差分を
  そのまま保存(JPEG 予測と一致)。
- **可逆性**: 復号した係数を標準アルゴリズムで再 Huffman 符号化し、元の
  エントロピーバイト列とビット一致することを保存前に検証。一致しない
  (非標準エンコーダ産)場合は不採用。読み出し時も SHA-256 で最終検証。
- **悪化回避ガード**: 係数平面を zstd-19 で圧縮した見込みサイズが元 JPEG
  以下の場合のみ採用(単画像で決して膨らませない)。

**採用判定の教訓**: 最初 probe に SpeedDefault を使ったところ、実写真
(libjpeg 産)を軒並み「縮まない」と誤判定して全て不採用になった。probe を
最高レベル(zstd-19 相当)に上げたところ、実写真で正しく採用されるように
なった — probe が保存経路の実圧縮(libzstd-19)より弱いと、実際には縮む
ものを取りこぼす。probe は実経路にできるだけ近づけるべき。

**実測**(libjpeg 産の実 JPEG、係数平面を zstd-19):

| 画像 | 元 | 分解+zstd19 | 削減 |
|---|---:|---:|---:|
| q85 写真(4:2:0) | 66.4 KB | 63.9 KB | -3.8% |
| q92 写真(4:4:4) | 221 KB | 206 KB | -6.8% |
| q55 低画質写真 | 34.1 KB | 31.6 KB | -7.5% |
| **スクショ(JPEG保存)** | 77.6 KB | 20.6 KB | **-73%** |
| q80 最適化Huffman | 44.8 KB | 48.1 KB | +7.3%(不採用=raw維持) |

ストア経路(libzstd-19)では合成写真で **-30.3%** を実測(probe より更に良い)。
すべてビット一致で復元・fsck 健全。ファザー(受理入力は必ずビット一致で
再構成できる不変量)を CI に追加。

**動画について(正直な評価)**: H.264/H.265/VP9 等の動画は既に動き補償+
変換+算術/CABAC 符号化で理論限界近くまで圧縮されており、可逆にさらに
縮める現実的な純Go手段は無い。動画で効くのは(a)同一/近似動画の重複排除
(チャンクレベルで既に有効)と(b)コンテナ(MP4)メタの dedup のみ。
JPEG のように「符号を可逆に取り出して並べ替える」余地が動画コーデックには
実質ない(これは情報理論的な壁)。過度な期待をさせないため明記する。

**単画像の限界**: 平滑な画像・スクショ・低画質・高品質写真ではプラスだが、
高ノイズかつ既に最適Huffman化された写真では zstd が Huffman を超えられず
不採用になる(算術符号+文脈モデルを使う lepton なら -22% 取れるが、
外部ビルドが要る)。最大の価値は**似た画像(バースト・再保存・サムネイル・
編集版)の dedup/デルタ**を可能にする点で、これは JPEG のエントロピー符号が
本来阻む効果。

## 4.18 追加研究(2026-07-18): JPEG 係数の文脈モデル+算術符号(lepton 級)

§4.17 の JPEG 再圧縮は、係数を平面化して zstd に渡す方式だった。しかし
実写真では zstd(LZ+FSE)が JPEG の Huffman をわずかしか下回れない
(-3.5〜-7%)。理由は zstd が「隣接ブロックの同じ周波数成分は相関する」
という**2次元の空間相関**を使えないため。lepton/packJPG が -22% を出すのは
この相関を**文脈モデル+算術符号**で突くからで、これを純Goで実装した。

**実装**(`internal/precomp/rangecoder.go`, `jpeg_model.go`):
- **二値レンジコーダ**(LZMA 型の適応二値算術符号)。単体検証で p=0.05 を
  0.299 bit/symbol(理論エントロピー 0.286 に近い)まで圧縮。
- **文脈モデル**: 各量子化 DCT 係数の大きさカテゴリを、
  文脈 =(成分, 周波数帯バケット, 左・上ブロックの同位置係数の活性度)で
  適応符号化する。ゼロ/非ゼロの予測(高周波はほぼゼロ)と、隣接ブロックが
  大きければ当該係数も大きい、という空間相関を突く。
- 可逆(復号は係数を完全再現)。保存前に**符号→復号の往復で係数一致を検証**
  してから採用(非対称バグを絶対に採らない=読み出し不能を防ぐ)。

**2コーダの自動選択**: 実測で「写真は算術符号が圧勝、スクショ・図は zstd の
LZ マッチが圧勝」と判明したため、両方を見積もって**小さい方を採用**する。

**実測**(実 store 経由・libzstd・全てビット一致復元):

| 画像 | §4.17(zstd) | §4.18(文脈算術) | 採用コーダ |
|---|---:|---:|---|
| q85 写真(4:2:0) | -2.8% | **-22.6%** | arith |
| q92 写真(4:4:4) | -6.6% | **-27.4%** | arith |
| q80 最適化Huffman | +8.0%(不採用) | **-13.2%** | arith |
| q85 リスタート | -2.6% | **-22.8%** | arith |
| スクショ(JPEG) | -72.6% | **-73.9%** | zstd(自動選択) |
| q55 低画質写真 | -5.6% | **-25.0%** | arith |

実写真で -22〜-27% は lepton(-22%)級。算術符号+文脈モデルを純Go・cgo
不要で実装したことで、外部ビルド許可なしにメディア圧縮の本丸に到達した。

**トレードオフ**: 算術符号のペイロードは密で、近似重複画像の dedup/デルタは
効きにくくなる(平面 varint 形式なら効く)。ただし単画像の -22〜-27% が
それを上回るため写真では算術を選ぶ。完全一致画像の dedup は生バイトでも
効くので損なわれない。スクショ・図は平面形式が選ばれ dedup も維持される。

**残る伸びしろ**は §4.19 で実装(マンティッサ文脈化・DC 勾配予測を採用、
EOB 明示化は実測で悪化したため不採用)。

## 4.19 追加研究(2026-07-18): 文脈モデルの精緻化(lepton 超え)

§4.18 の「残る伸びしろ」を実データ(実写真6枚)で一つずつ測定し、
利得が確認できたものだけを採用した(パターン: 実測して縮む時だけ採る)。

| 施策 | 内容 | 実測(合計削減率) |
|---|---|---:|
| ベースライン(§4.18) | | -31.7% |
| ①マンティッサ文脈化 | マンティッサ各ビットを(成分,帯,カテゴリ,ビット位置)で符号化(0.5 固定をやめる) | -32.9% |
| ②活性度の精緻化 | 隣接に**左上ブロック**を追加、活性バケットを 4→6 段に | -33.5% |
| ③**DC 勾配予測** | DC を絶対値に復元し **JPEG-LS 中央値予測子**(左・上・左上)で空間予測、残差を符号化 | **-36.3%** |
| ④帯文脈の精緻化 | 低周波 k=1..8 を各位置独立の文脈に(帯 7→12 段) | **-37.0%** |

- **③ DC 勾配予測**が最大の利得(+2.8pt)。JPEG 標準の DC 差分は「走査順で
  一つ前のブロック」との 1 次元差分だが、これを絶対 DC に戻して**2次元の
  空間予測**(中央値予測子は予測値が [min,max] に収まり残差が発散しない)に
  置き換えた。復号側は残差から絶対 DC を復元し、走査順差分に戻して
  再 Huffman するため**ビット一致**を保つ。
- 一方 **EOB(非ゼロ数)の明示的モデル化は実測で悪化**(-36.3%→-33.5%)し
  不採用。位置ごとの EOB ビットが、文脈付き大きさ終端ビットと冗長になり、
  高周波が密な高画質写真ほど損をするため。lepton は num-nonzeros を先に
  符号化する別構成でこれを回避しているが、本実装の per-position 文脈では
  終端ビット方式の方が効率的だった。
- 適応速度(LZMA moveBits)は 4/5/6 を実測し **5 が最良**(標準値)。

**実測**(実 store 経由・libzstd・全てビット一致復元、§4.18 と同じ6枚):

| 画像 | §4.18 | §4.19 | lepton 目安 |
|---|---:|---:|---:|
| q85 写真(4:2:0) | -22.6% | **-29.5%** | ~-22% |
| q92 写真(4:4:4) | -27.4% | **-32.5%** | ~-22% |
| q80 最適化Huffman | -13.2% | **-22.1%** | — |
| q85 リスタート | -22.8% | **-29.9%** | ~-22% |
| q55 低画質写真 | -25.0% | **-33.6%** | — |
| スクショ(JPEG) | -73.9%(zstd) | -73.9%(zstd) | — |

写真の削減率が **-22〜-27% → -29〜-34%** に向上し、lepton(平均 -22%)を
明確に上回った。純Go・cgo 不要・往復検証つきは §4.18 から不変。

**残る伸びしろ**: 色成分間予測(Cb/Cr の相関)、量子化テーブルを見た
帯重み付け、文脈混合(複数モデルの加重)など。現状で既に lepton 超えの
ため優先度は下げ、まず実運用での安定性を優先する。

## 4.20 追加研究(2026-07-18): 全データ種別の後段圧縮 — brotli ベストオブ + BCJ

画像特化(§4.17〜19)に続き、「あらゆるデータ」に効く後段(エントロピー)
圧縮の強化を測定した。候補は純Goで運用できる xz/LZMA2(ulikunitz/xz)、
brotli 品質10/11(andybalholm/brotli、大窓 lgwin=24)。ベースラインは
現行オフライン経路の libzstd-22 + 大窓 + LDM。代表5種のコーパス
(ランダムフィールド入りログ 11MB / JSONL 6MB / 数値CSV 6MB /
Goソース連結 474KB / Go実行バイナリ 11MB)での実測:

| データ | libzstd-22+LDM | xz(lzma2) | brotli-10 | **brotli-11** |
|---|---:|---:|---:|---:|
| ログ | 15.24% | 20.72% | 15.30% | **14.75%** |
| JSONL | 8.17% | 11.25% | 8.87% | **8.12%** |
| CSV | 25.01% | 31.94% | 26.00% | **24.29%** |
| ソース | 23.16% | 25.55% | 22.76% | **22.32%** |
| バイナリ | 49.24% | 52.04% | 48.70% | **47.77%** |

(数値は元サイズ比。小さいほど良い)

- **brotli-11 が全5種で libzstd-22 を下回った**(相対 -0.7〜-4%)。
  一方 **xz/LZMA2(純Go実装)は全種で zstd-22 に負けた**ため不採用
  (C の xz -9e なら勝つ領域もあるが、外部依存なしという制約の中では
  brotli が明確に優る)。
- brotli の圧縮は zstd-22 の 3〜5 倍遅いが、**伸長は zstd 並みに速い**
  (11MB を 47〜80ms)。「圧縮は一度・読みは何度でも」のオフライン再圧縮
  (Optimize)にだけ使えば遅さはユーザーに見えない。
- **BCJ(x86 E8/E9 相対→絶対変換)フィルタ**を実行バイナリに前置すると、
  zstd でも brotli でもさらに **-1.5〜-1.6%**。同じ関数への CALL が同じ
  バイト列になり LZ が拾えるようになるため(xz の x86 フィルタと同原理)。
  変換は完全可逆(走査判定に使うオペコード位置のバイトは書き換えない)で、
  ファジング(FuzzBCJRoundTrip)でも恒等性を検証している。

**実装**(`internal/store/offline_codec.go`): オフライン経路(チャンク
再圧縮・リージョン圧縮)の最終段を「libzstd-22 / brotli-11 /(機械語らしい
データのみ)BCJ+両者 の候補を全部実測して最小を採用」のベストオブに変更。
- 採用は常に「現表現より厳密に小さい」場合のみ=物理容量は単調減少。
- zstd 以外の候補は**採用前に往復検証**(復号して bytes.Equal)。読み出し
  不能な表現は決して採らない。
- 保存表現タグ(ChunkMeta.Compression)に `br` / `zstd-bcj` / `br-bcj` を
  追加。リージョンはメンバーのタグがリージョン全体の形式を表す。
- brotli は純Goのため、**cgo なしビルドでもオフライン再圧縮の利得が出る**
  ようになった(従来は libzstd が使える場合のみ再圧縮していた)。
- BCJ の門番ヒューリスティック(NUL 率 ≥2%・E8/E9 率 0.1〜8%)は CPU の
  節約のためだけにあり、誤検知しても best-of 判定が悪化を弾く。

**採らなかったもの**: xz/LZMA2(上記の通り全敗)。brotli-10(速いが
テキスト系で zstd-22 に負ける。best-of の片翼としては 11 のみで十分)。
辞書学習(§4.15 で不採用済み)。

**適用漏れの修正(同日)**: サーバー経路で取り込まれたチャンクは
DeltaTried=true で登録されるためデルタパス経由の再圧縮が走らず、
リージョンにも入らなかったチャンク(中サイズの単独チャンク等)が
取り込み時の zstd-19 のまま残っていた。全独立チャンクを一度だけベスト
オブ再圧縮する独立パス(RecompressTried フラグで冪等)を Optimize の
リージョン構築後に追加し、この漏れを塞いだ。

**JPEG 色成分間予測の実験(否定的結果)**: Cr の文脈に符号化済み Cb の
同位置係数を(a)活性への加算、(b)独立次元(非ゼロフラグで文脈分岐)の
2方式で追加して実測したが、いずれも合計 -37.03% → -37.00/-36.98% と
僅かに悪化し不採用。クロマ間相関は空間文脈が既に捉えている情報と重複し、
文脈分割による適応の遅れが上回るため。lepton も色成分間予測は行って
いない(輝度→クロマのブロックサイズ差もあり効果が薄い)。

## 4.21 追加研究(2026-07-18): GIF の LZW アンラップ+小ファイルソリッドの拡大

**GIF**(画像圧縮率の追加改善): GIF の画像データは 1987 年の LZW 圧縮で、
現代のエントロピー圧縮より大幅に弱い。JPEG(§4.17〜19)と同じ発想で
LZW を復号し「スケルトン+生ピクセル列」に分解して保存する方式を実装した
(`internal/precomp/gif.go`、純Go・cgo 不要)。

- **可逆性の保証**: 復号ピクセルを compress/lzw で再符号化し、サブブロック
  分割まで含めて**元とビット一致する画像だけ**アンラップ(部分適用)。
  スパイク実測では Go/giflib 系エンコーダ産は一致、PIL の写真調 GIF は
  2バイト差で不一致 → 安全に素通しされる。復元時は SHA-256 最終検証つき。
- **実測**: 単フレーム GIF probe -75%(12.8KB→3.2KB)、アニメ5フレーム
  **-89%**(64KB→6.8KB。フレーム間の相関がソリッド圧縮で拾える)。
  実 store 経由ではアニメ GIF が **7.1x**(-86%)・ビット一致。
- ファジング(FuzzTryUnwrapGIF、79万実行)で panic・不一致なし。CI 組込済。

**小ファイルのソリッド圧縮拡大**: 小チャンクリージョンの最大メンバー数を
64→**256** に拡大。2000 個の小さな JSON(平均 825B)で実測:

| 構成 | 削減率 |
|---|---:|
| 従来(64メンバー、28リージョン) | 7.39x |
| **256メンバー(7リージョン)** | **7.72x** |
| 512メンバー(4リージョン) | 7.76x |
| 参考: 全ファイル連結+brotli-11(理論上限近似) | 8.93x |

512 は 256 比 +0.5% しか上乗せがなく、1ファイル読み出しの伸長量
(読み出し増幅)が倍になるため 256 を採用。理論上限との残差は
リージョン独立性(7つの独立辞書)によるもので、これ以上の統合は
読み出し増幅・回収(コンパクション)コストとのトレードオフになる。

**動画について(再確認)**: H.264/H.265/VP9/AV1 は算術符号(CABAC 等)で
既に情報理論的限界近くまで圧縮されており、可逆にさらに縮める余地は
実質ない(lepton の JPEG に相当する「Huffman の置換」の余地がない)。
完全一致の重複排除(同じ動画の再アップロードは物理1回分)だけが有効で、
これは既に機能している。

## 4.22 関連研究の調査(2026-07-18 検索)

自作技術の位置づけ確認と次の伸びしろ探索のため、外部の関連研究を調査した。

**JPEG 可逆再圧縮の到達点**:
- [Lepton](https://dropbox.tech/infrastructure/lepton-image-compression-saving-22-losslessly-from-images-at-15mbs)(Dropbox)は Huffman を並列算術符号に置換して **-22%**。
- [Brunsli](https://arxiv.org/pdf/2506.05987)(Google、JPEG XL の前身)も約 **-22%**。
- 学習型([Multi-Level Cross-Channel Entropy Model](https://arxiv.org/pdf/2203.16357)、
  [Efficient Learned Lossless JPEG Recompression](https://arxiv.org/pdf/2308.13287))は
  さらに数%上乗せするが、NN 推論ランタイム(実用は GPU)が必要。
- 本実装(§4.19)の実写真 **-29〜-34%** は非学習型(lepton/brunsli 級の
  手法クラス)として妥当な上位水準にあり、これ以上は学習型の領域になる。
  CPU 単体・純Go・依存ゼロという本サービスの制約では現状が概ね上限と判断。

**ニューラル可逆圧縮(汎用)**:
[サーベイ(2026-01)](https://www.eurekalert.org/news-releases/1111971)や
[軽量予測器チェーン(2026)](https://arxiv.org/pdf/2604.15472)、
[EDPC(2025)](https://arxiv.org/pdf/2507.18969) など、NN ベース可逆圧縮の
実用化研究が活発。圧縮率は zstd/brotli を大きく超えるが、スループットは
GPU 前提でも MB/s 級であり、CPU のみのストレージサーバーの背景処理には
まだ不適。**ハードウェア前提が変わったら再評価**する項目として記録。

**モデル特化 dedup**: [ZipLLM(2025)](https://arxiv.org/html/2505.06252v3) は
LLM チェックポイント群の構造を使ったデルタ圧縮で、汎用 CDC dedup が
モデル保存で最適でないことを示す。本サービスにモデル保存需要が出たら
テンソル境界チャンキング(構造認識 CDC)が有効という示唆。

**zstd/brotli の使い分け**: [Web 圧縮の 2026 年時点の比較](https://blog.andr2i.com/posts/2026-06-03-web-compression-in-2026-brotli-zstd-and-compression-patterns)でも「自分のデータで実測して選ぶ」が推奨されており、
本実装の**ベストオブ方式(§4.20)は両者の使い分け問題を実測で自動解決**する
設計として整合している。

## 4.23 動画圧縮の本格研究(2026-07-18)

「動画の圧縮率が低すぎる」に対する全力の研究。結論を先に:
**現代コーデック(H.264/H.265/VP9/AV1)の可逆再圧縮は原理的に余地がほぼ
ない**が、**MJPEG(Motion JPEG)は当方の JPEG 文脈算術コーダがそのまま
効く実在のニッチ**であり、これを実装した(実測: 動画で −46%)。

### 4.23.1 なぜ H.264 以降は縮まないか(機会分析)

JPEG が lepton 級に -30% 縮むのは、エントロピー段が **Huffman**(シンボル
あたり最低1ビット・静的テーブル)だから。一方:

- H.264 Main/High 以降の **CABAC** は適応二値算術符号で、当方が JPEG 用に
  実装したレンジコーダと**同じ原理**。置き換える先がもう使われている。
- H.264 Baseline の **CAVLC** は VLC(Huffman 類似)で理論上 10-15% の
  余地があるが([CABAC vs CAVLC 比較](https://www.researchgate.net/publication/320989297_A_comparative_evaluation_between_CABAC_and_CAVLC))、
  可逆再符号化には H.264 のシンタックス全体(スライス・マクロブロック・
  予測モード・動きベクトル)の完全なパーサ+CABAC 実装が必要で、
  lepton に対する jpeg.go(数百行)と違い数万行規模。既存ビットストリーム
  の可逆エントロピー再符号化ツールは**世界的にも実用例がない**
  (研究は[エンコーダ内部の改良](https://www.sciencedirect.com/science/article/abs/pii/S0923596510000470)に留まる)。
- 学術の「lossless video coding」([例](https://www.researchgate.net/publication/251313025_Efficient_Differential_Pixel_Value_Coding_in_CABAC_for_H264AVC_Lossless_Video_Compression))は
  「画素を可逆にエンコードする」話であり、「既存の非可逆ストリームを
  可逆に縮める」話ではない(用語が紛らわしいが別問題)。
- 動画の重複排除研究([MLT-Dedup 等](https://arxiv.org/html/2606.12215v1))は
  近似重複の**検出**(アプリケーション層・非可逆)であり、ビット一致保存
  のストレージには適用できない。

**当ストアで既に効くもの**: 完全一致 dedup(同一動画の再アップロード)、
部分一致 dedup(同一エンコードのトリミング・連結は CDC が共有部分を拾う)、
コンテナ再多重化(re-mux)されたファイルのメディア部分一致。

### 4.23.2 実装: MJPEG(AVI)のフレーム単位 JPEG 分解

MJPEG は「baseline JPEG の連続」で、監視カメラ・ウェブカメラ・ドローン・
科学計測(顕微鏡・ハイスピードカメラ)で現役。1フレームごとに JPEG
文脈算術コーダ(§4.18-19)を適用できる。

実装(`internal/precomp/mjpeg.go`): AVI(RIFF)コンテナを歩き、movi LIST
内の JPEG フレーム(##dc/##db)をフレーム単位で TryUnwrapJPEG にかける。
フレームごとにビット一致検証(一致しないフレームは素通し=部分適用)、
全体でも probe ゲート、復元時 SHA-256 最終検証。純Go・cgo 不要。

実測(実 store 経由・ビット一致復元):

| 対象 | 削減率 |
|---|---:|
| 独立写真4フレームの AVI(probe 見積り) | **-27.4%** |
| 動画的な12フレーム(隣接フレームが似ている) | **-46.2%**(1.86x) |

動画的なフレーム列では、フレーム単独の -27% に加えて**フレーム間の類似が
係数平面の dedup/デルタ/ソリッド圧縮で拾われ**、削減が -46% まで伸びる。
ファジング(FuzzTryUnwrapAVI)を CI に組込。

### 4.23.3 到達点と残る選択肢

| 動画形式 | 当ストアの現状 | 残る余地 |
|---|---|---|
| **MJPEG (AVI)** | **-27〜-46%**(本節) | MOV コンテナ対応(同じフレーム分解を流用可能) |
| H.264 CABAC / H.265 / VP9 / AV1 | dedup のみ(~1.0x) | 原理的にほぼゼロ |
| H.264 CAVLC (Baseline) | dedup のみ | 理論 10-15%。要 H.264 完全パーサ(数万行)= 費用対効果から見送り、フロンティアとして記録 |
| 生/RLE AVI・非圧縮フォーマット | zstd/brotli が通常どおり効く | — |
| アニメ GIF(動画の代替でよく使われる) | **-86%**(§4.21) | — |

「動画が縮まない」の正体は「H.264 以降のストリームは**保存前から既に
算術符号で最適圧縮されている**」こと。当ストアは膨張させず(raw 素通し)、
重複だけを排除するのが情報理論的に正しい振る舞いであり、その上で
縮められる動画形式(MJPEG・GIF)は本節までで全て実装した。

## 4.24 追加研究(2026-07-18): プログレッシブ JPEG の可逆再圧縮

Web・スマホ写真の相当割合(mozjpeg/PIL の既定は品質次第でプログレッシブ)
がプログレッシブ JPEG で、従来は素通し(1.0x)だった。baseline 用の文脈
算術コーダ(§4.18-19)は**係数さえ取り出せばそのまま効く**ので、全スキャンを
復号して完全係数に復元し、圧縮する経路を実装した。

**プログレッシブの構造**: baseline が1スキャンで全係数を送るのに対し、
プログレッシブは複数スキャンで段階的に送る:
- DC first / DC refine(逐次近似で DC を1ビットずつ)
- AC first(周波数帯 Ss..Se を初期振幅で、EOBRUN 付き)
- AC refine(既存係数の補正ビット+新規有意係数、逐次近似)

**実装**(`internal/precomp/jpeg_prog.go` / `jpeg_prog_codec.go`):
ITU T.81 G 節 = libjpeg の decode/encode_mcu_AC_first/refine を純Go で移植。
全スキャンを復号 → 完全係数配列(baseline と同じ MCU 順レイアウト)→
文脈算術符号化。**可逆性は全スキャンのビット一致再符号化で保証**
(libjpeg/mozjpeg/PIL は標準アルゴリズムなので一致。一致しなければ素通し)。

**移植で嵌った点(記録)**: AC refine のゼロラン(ZRL)処理で、内側の
補正ループを抜けた後の `k++` を「新規係数を置いたときだけ」にしていたため、
ZRL(新規係数なし・16ゼロスキップ)で位置がずれて後続スキャンが desync
した。libjpeg の外側 for の `k++` に合わせ**常に1つ進める**ことで解決。
ビット一致検証ガードがあるため、この種のバグは「不採用(素通し)」に
落ちるだけでデータ破損にはならない(実際、修正前は adopt=false だった)。

**実測**(PIL 生成の実プログレッシブ、実 store・ビット一致復元):

| 画像 | スキャン数 | 削減率 |
|---|---:|---:|
| q85 写真(4:2:0) | 10 | -10.6% |
| q92 写真(4:4:4) | 10 | -12.9% |
| q85 リスタート付き | 10 | -22.1% |
| q75 小画像 | 10 | -3.6% |

baseline(-29〜-34%)より低いのは、10スキャンぶんの SOS/DHT ヘッダの
固定オーバーヘッドが小さい画像で相対的に大きいため。係数データが支配的な
大きい写真ほど baseline に近づく。ヘッダ(スケルトン)はチャンク化内容に
含まれ store の zstd/brotli で更に縮み、似た画像間で dedup も効く。

これで **JPEG は baseline / プログレッシブ両方**に対応し、実運用の写真の
ほぼ全てが圧縮対象になった。FuzzTryUnwrapProgressive を CI に追加。

## 4.25 追加研究(2026-07-18): PCM 音声(WAV)の可逆圧縮 — 素通しの解消

6次元の調査ワークフロー(48エージェント・敵対的検証つき)で最優先と判定
された案。**非圧縮 PCM 音声は従来 1.0x で raw 保存されており大きな
取りこぼし**だった。動画(§4.23)が「既に算術符号済みで縮まない」のと対に、
PCM は生サンプルなので FLAC/Shorten と同じ発想で縮む。

**実装**(`internal/precomp/pcm.go`): WAV(RIFF/WAVE)の fmt/data を解析し、
整数 PCM(fmt=1、extensible 0xFFFE の PCM SubFormat 含む、8/16/24/32bit)の
サンプルを:
1. チャンネルごとに分離(相関を高める)
2. **固定線形予測子**(Shorten 系、次数0〜3、特許フリー)を各チャンネルに
   かけ、残差の絶対値和が最小の次数を選ぶ
3. 残差(mod 2^bits)を**バイト平面**(チャンネル→バイト位置→フレーム)に
   並べ替え、store の zstd/brotli に圧縮させる

**安全性(最も安全な変換)**: 予測↔逆予測は 2^bits の整数環での完全な
全単射(丸めなし)で、エンコード/デコードの両方向を自前で持つ。JPEG や
gzip のように「第三者エンコーダの出力にバイト一致させる」必要がないため、
原理的に往復不能があり得ない。加えて「復元見込みが元より小さい時だけ採用」
+ 読み出し時 SHA-256。float PCM(fmt=3)・雑音は不採用で素通し。

**実測**(実 store・ビット一致復元・Optimize 後も一致):

| 音声 | 予測次数 | 削減率 | raw+zstd 比 |
|---|---:|---:|---|
| 16bit ステレオ(倍音+包絡) | 1 | -24.8% | 173KB→133KB |
| 16bit モノ 音声様(無音区間あり) | 1 | -36.1% | 53KB→41KB |
| 24bit ステレオ(低エントロピー) | 3 | -95.7% | 16KB→5.6KB |
| ラウド(ほぼ雑音、最悪ケース) | — | 不採用(正しく素通し) | — |

残差をバイト平面に並べると高位バイト(ほぼ 0/FF)が固まり zstd が高率で
畳む。似た音源(別テイク・別マスター)同士では残差の dedup/デルタも効く。
FuzzTryUnwrapWAV を CI に追加。

**残る伸びしろ**(記録): ブロック単位の次数選択(無音/有音で切替)、
残差の専用エントロピー符号(Rice/レンジ)、AIFF(ビッグエンディアン)、
MOV/MP4/AVI 内の PCM トラック抽出。現状の「予測+バイト平面+store圧縮」で
整数 PCM の主要価値は取れている。float PCM は相関が低く利得僅少のため見送り。

## 4.26 追加研究(2026-07-18): 非圧縮 BMP の行予測フィルタ

非圧縮 BMP(BI_RGB)は生ピクセルがそのまま並ぶだけで空間相関を使っていない
(PNG が小さいのはこの相関を行フィルタで除くから)。BMP のピクセル配列に
**PNG と同じ行ごとの適応予測フィルタ**(None/Sub/Up/Average/Paeth を行ごとに
最小残差で選択)をかけて残差にし、store の zstd/brotli に圧縮させる。

**実装**(`internal/precomp/bmp.go`): BITMAPINFOHEADER 以降を解析し、
非圧縮(comp=0)・8/24/32bpp のピクセル配列に行フィルタを適用。フィルタ↔逆
フィルタは 256 剰余環の全単射で往復不能があり得ない安全な変換。行ごとの
フィルタ種別(1バイト/行)をレシピに持つ。トップダウン/ボトムアップや
パレット・ICC は prefix/suffix として保存され不問。

**実測**(実 store・ビット一致復元):
- 写真調 BMP: **-50.8%**(生ピクセル→フィルタで相関除去→zstd)
- 合成ノイズ BMP: -34.9%(raw+zstd はほぼ 0% だった)

**非圧縮 TIFF(追記)**: 同じ行フィルタを非圧縮(Compression=1)・8bit・
chunky の TIFF ストリップに適用。ストリップは散在しうるので GIF/MJPEG と
同じセグメント方式で原位置に戻す。実測: 写真調 TIFF **-54.7%**、合成 -35.1%。
LZW/PackBits/deflate 圧縮 TIFF は非対象で安全に素通し(それらの弱い符号を
剥がすのは別途)。

## 4.27 追加研究(2026-07-18): 文脈混合コーダの評価(否定的)とブロック適応音声予測

**文脈混合コーダ(lpaq-lite)の評価 — 不採用**: 複数次数(order 0..4)の
ビット予測をロジスティック混合して二値レンジ符号化するコーダを試作し、
実データで brotli-11 と比較した:

| データ | CM | brotli-11 |
|---|---:|---:|
| log.txt | **12.1%** | 15.2% |
| metrics.csv | **19.3%** | 24.5% |
| data.jsonl | 8.4% | 8.2% |
| source.go.txt | 29.2% | 22.3% |
| binary | 40.6% | 34.9% |

ログ・CSV(反復的な記録型テキスト)で brotli を相対 20% 上回るが、
**復号が ~1 MB/s と遅すぎる**(文脈混合の宿命。zstd は ~1 GB/s)。
読み出しを提供するストレージでは、たとえキャッシュがあってもコールド読み
の初回が致命的に遅くなる(64MiB リージョン=64秒)。JSON・コード・
バイナリでは brotli/LZ に負ける場合もある。**採用しない**(zstd/brotli が
文脈混合を使わないのと同じ理由)。純粋な深冷アーカイブ専用ティアなら
価値があるが、別途の階層化機構が要る。研究結果として記録。

**ブロック適応の固定予測(音声、採用)**: PCM 音声(§4.25)の固定線形予測を
チャンネル一律から **4096 サンプルのブロックごとに最良次数を選ぶ**方式に
変更(履歴は連続なのでブロック境界でも予測は途切れない)。無音/有音や
静→動で最適次数が変わる音声(スピーチ・ポッドキャスト)で縮む。実測:
スピーチの残差コスト -14.7%、実 store で -36.1%→-36.8%。定常音(音楽)は
不変。ブロックごとの best-of なので決して悪化しない。復号は高速のまま。

**適応 LPC(音声、two-candidate で採用)**: 固定予測(次数0〜4)に加え、
ブロックごとに **Levinson-Durbin で自己相関から求めた次数8の LPC 係数を
整数量子化(符号付き12bit・右シフト)**した予測子を候補に足した。LPC 単体は
残差の絶対振幅を確かに下げる(実測: 音楽/スピーチ残差 -22〜-30%)が、
**素朴に「常に LPC を許可」すると音楽が -24.8%→-24.2% に悪化**した。原因は
バイト平面+zstd 経路が固定予測の残す反復構造(LZ で拾える)に依存しており、
LPC の白色化がその構造を消すため。残差コスト(絶対振幅)最小 ≠ zstd 後最小、
という予測器選択の落とし穴。

対策として **2候補を最後まで作って実測で選ぶ**方式にした:
(a)「固定のみ」でバイト平面化したもの、(b)「固定+LPC の best-of」でバイト
平面化したもの、の両方を probe 圧縮し**小さい方だけを採用**する。これで LPC が
有利な音源だけが LPC を使い、不利な音源は固定にフォールバックするので
**回帰が構造的に不可能**になる。実測(store 経路):

| 音源 | 固定のみ(従来) | two-candidate(採用) |
|---|---:|---:|
| a_speech.wav(16bit スピーチ) | -36.8% | **-38.5%**(LPC 勝ち) |
| a_music.wav(16bit 音楽) | -24.8% | **-24.8%**(固定にフォールバック) |
| a_24bit.wav | -95.9% | -95.9% |
| a_aiff.aiff | -88.0% | -88.0% |

**教訓**: 予測器のコスト関数は「残差の絶対振幅」ではなく「**後段圧縮器を通した
実バイト数**」で評価すべき。振幅最小化と LZ 圧縮性は一般に一致しない。安全側の
実装は候補を全部作って実測で選ぶこと(コストは probe 1回分の zstd のみ)。

## 4.28 次の大物: MP3 エントロピー再圧縮(準備完了・未着手)

MP3(世界最大級のロスあり音声コーパス)は量子化 MDCT 係数を 34 個の固定
Huffman テーブルで符号化しており、**JPEG と同じ「Huffman→文脈算術」構図**で
-8〜16%(packMP3 級)の可逆再圧縮が見込める。復号は速い(読み出し時に
再 Huffman するだけ)ので CM と違い読み出し性能の問題はない。実装の要点:
- フレーム同期・side_info 解析・**ビットリザーバ**(main_data_begin による
  フレームをまたぐ main_data 再構成)が最大の複雑さ。
- スケールファクタ+スペクトル値の Huffman 復号 → 既存 rangecoder で文脈
  算術符号化 → **再 Huffman がビット一致する場合のみ採用**(既存の安全弁)。
検証基盤は準備済み(lameenc で実 MP3 を生成可能)。約 500 行の集中実装を
要するため、独立したセッションで着手する。

## 4.29 追加研究(2026-07-19): 汎用コーデックの探索 — bzip2(BWT)採用、LZMA は不採用

「全データ方式の圧縮率を極限まで向上させる」ため、オフライン best-of
(現状 zstd-22 / brotli-11 / +BCJ)に**別原理の汎用コーデック**を足せるか
実データで検証した。probe は保存経路と同一(brotli lgwin=24, zstd level22)。

**LZMA / xz(純Go: ulikunitz/xz)— 不採用**: C 版 liblzma は一般に brotli を
上回るが、純Go 実装はマッチファインダ・最適パースが弱く、実測で **全種別で
brotli-11 / zstd-22 に負けた**:

| データ(4MiB) | zstd-22 | brotli-11 | xz/lzma |
|---|---:|---:|---:|
| log | 614,856 | **569,441** | 802,878 |
| text | **1,200,912** | 1,218,768 | 1,401,387 |
| csv | 1,319,239 | **1,247,656** | 1,611,981 |
| source.go | 130,561 | **125,703** | 145,148 |

cgo で liblzma に繋げば勝てる可能性はあるが、依存が重く伸長も遅いため見送り。

**bzip2 / BWT(dsnet 圧縮・標準ライブラリ伸長)— 採用**: Burrows-Wheeler 変換は
LZ とは異なる冗長性(局所的な文脈のソート)を突くため、**反復的な自然文・ログ
で brotli を上回った**:

| データ(4MiB) | best-of(zstd/br) | bzip2 | 差 |
|---|---:|---:|---:|
| **text(疑似自然文)** | 1,200,912 | **1,091,961** | **-9.1%** |
| **log** | 569,441 | **562,182** | -1.3% |
| csv | 1,247,656 | 1,309,480 | +5%(不採用) |
| source.go | 125,703 | 127,473 | +1%(不採用) |
| a_music.wav | 157,819 | 177,648 | +13%(不採用) |

text -9% は大きい(自然文書・ログは中核データ種別)。best-of なので**負ける
データでは採用されず悪化はあり得ない**。

**設計上の判断**:
- **伸長は標準ライブラリ `compress/bzip2`**(純Go・decode 専用・常在)を使う。
  採用前の往復検証もこの経路で行うので、**検証経路=読み出し経路**が一致し、
  CGO 無効ビルドでも読み戻せる。エンコードのみ dsnet(offline 専用)。
- **伸長速度 ~16MB/s**(実測)は brotli(~400MB/s)より遅いが、§4.27 で却下した
  文脈混合(~1MB/s)とは桁が違い、オフライン層(採用チャンクは伸長済み LRU
  キャッシュが吸収)なら許容。ただしレイテンシを抑えるため:
  - **入力上限 12MiB**(bzip2 のブロックは 900KiB で窓が伸びず、巨大ソリッド
    リージョンでは brotli の大窓が勝つので、上限外を試す価値も薄い)。
  - **最小マージン 1.5%**(伸長が遅いぶん「ある程度縮む時だけ」採用して
    レイテンシコストを実利で相殺)。
- 保存タグ `bz2`、往復検証つき。fuzz(`FuzzBzip2Decode`)で任意入力の
  無パニック・上限厳守を担保。

**教訓**: 単一の最強 LZ(brotli)に BWT を**併置**するだけで、LZ が苦手な
反復自然文を数%取り返せる。「一つの万能コーデック」より「原理の違う複数を
best-of」が容量には効く(csv/コード/音声では BWT が負けるのも同じ理由=
best-of が正しく振り分ける)。

## 4.30 独自研究(2026-07-19): CSV/区切りテキストの列指向(columnar)可逆変換

汎用コーデック(§4.29)は「バイト列としての冗長性」を突くが、**構造を持つ
データはその構造を明かすと桁違いに縮む**。CSV/TSV は明確な構造(行 × 列)を
持つのに、行指向のまま圧縮すると異種フィールド(タイムスタンプ・列挙・数値・
文字列)が交互に来て圧縮器を惑わせる。列ストア(Parquet 等)が小さいのと
同じ原理で、**列指向に転置**すれば各列が同種になり LZ/エントロピー符号が
効く。これは既存コーデックの探索ではなく、ashuku 独自の**構造認識前処理**。

**転置の可逆性(核心)**: 「'\n' で行 split → ',' で列 split → 列順に再連結」。
Go の `bytes.Split`/`Join` は完全な逆変換なので、**矩形(全行が同じ列数)で
ありさえすれば、引用符・CRLF の '\r'・非印字バイトもフィールド内容として
そのまま往復する**(RFC4180 の引用を解釈しない=解釈しないから壊れない)。
末尾改行の有無だけレシピに記録。加えて **数値列の delta 符号化**(その列の
各値が int64 に正準往復する時だけ、隣接差に置換)で近単調な ID・タイムスタンプ
列がさらに縮む。

**実測(疑似アクセスログ CSV 4MiB、store 最強 best-of)**:

| 変換 | サイズ | 行指向比 |
|---|---:|---:|
| 行指向(原文) | 956,342 | — |
| 列指向(転置のみ) | 747,432 | **-21.8%** |
| 列指向 + 数値列 delta | 715,517 | **-25.2%** |

end-to-end(ingest→転置→チャンク→best-of→読み戻し)でも**物理 -25.0%**を実測。

**安全設計**(4重):
1. **矩形性チェック**: 全行が同じ列数(≥2)、行数 ≥8。非矩形は素通し。
2. **best-of**: 転置のみ / 転置+delta を probe 圧縮し、かつ**行指向 probe より
   小さい時だけ**採用(delta が不利な列構成では転置のみ、それも不利なら素通し)。
3. **復元バイト一致検証**: 採用前に `ReconstructCSV` して orig と `bytes.Equal`。
4. **読み出し時 SHA-256**: 全 precomp 共通の最終検証。
delta 列は「parse→format が元バイトと一致(先頭ゼロ・'+'・"-0" は不可)」の
正準 int のみ対象なので、桁表現も完全再現。fuzz(`FuzzTryUnwrapCSV`)で任意
入力に対し「採用されたら必ずバイト一致復元」を担保。

**検出**: CSV は magic を持たないので、先頭 256B の先頭行が「印字可能 + ','
含む」時だけ全体をバッファ(誤検出しても矩形性 + best-of + 復元検証で無害に
素通し。コストは CPU のみで precompMax + 並行数制限が上限)。

**今後**: CRLF を行区切りとして扱う版(現状は '\r' をフィールド内容として往復)、
JSONL の同一スキーマ・レコード転置(→ §4.31 で実装)、辞書列(低カーディナリティ
列の ID 化)。

## 4.31 独自研究(2026-07-19): JSONL(構造化ログ)の骨格分離+列指向変換

CSV(§4.30)の「構造を明かして転置する」発想を **JSONL(改行区切り JSON、
構造化ログの事実上の標準)** に広げた。JSONL は各行が「同じ**骨格**(キー名・
区切り・空白)+ 異なる**値**」でできている。そこで:

1. 1行を **骨格**(値の範囲を 0x00 に置換した行)と **値の並び** に分解。
2. 全行の骨格が同一なら、**骨格を1つだけ**保存し、値を列指向に転置。
3. 数値列は CSV と同じ delta 符号化。

**可逆性の核心(意味解釈しない)**: 骨格は「その行から値バイト範囲を 0x00 に
置換しただけ」。JSON の1行に生の 0x00/0x0A は出ない(出るならエスケープ)ので、
「骨格を 0x00 で split して値と交互に連結」で**必ず元の行に戻る**。値の中身
(文字列エスケープ・数値表現・Unicode)を一切解釈しないので壊しようがない。
骨格が同一かつ値数が一致する行だけを対象にし、非対象(入れ子・配列値・
スキーマdrift・空白違い)は素通し。

**実測(疑似 JSON アクセスログ 3.4MiB、store 最強 best-of)**:

| 変換 | サイズ | 行指向比 |
|---|---:|---:|
| 行指向(原文) | 340,951 | — |
| 骨格+列指向(+数値 delta) | 252,412 | **-26.0%** |

end-to-end(実ストア)でも**物理 -27.4%**(CSV の -25.0% を上回る。JSON は
キー名の反復ぶん行指向で無駄が多く、骨格1本化がよく効く)。

**安全設計**は CSV と同じ4重(骨格一致チェック / best-of probe / 復元
バイト一致検証 / 読み出し SHA-256)。`skeletonizeFlat` は文字列エスケープ
(`\"`,`\\`)を正しく越えて値範囲を切り出す。fuzz(`FuzzTryUnwrapJSONL`)で
任意入力の「採用されたら必ずバイト一致復元」を担保。

**ディスパッチ順**: JSONL 行はカンマも含むので CSV 判定にも掛かる。先に JSONL
を試し、失敗時のみ CSV にフォールバックする(どちらも best-of + 復元検証つき
なので誤振り分けでも無害)。

**教訓**: 構造化データは「値の集合」と「構造(骨格)」を分けるだけで、構造の
反復が丸ごと畳める。**意味を解釈せず構造だけ触る**(0x00 プレースホルダ+
逐語骨格)ことで、JSON の複雑なエスケープ規則に一切踏み込まずに完全可逆を
達成できる — これが安全な構造変換の鍵。

## 4.32 CI 高速化とその過程で発見・修正した並行バグ(2026-07-19)

「Actions の実行が遅すぎる」を受けて CI(と `go test`)を高速化した。副産物として
**依存ライブラリの実データ競合を発見・修正**した。

**CI 壁時計 ~12分 → ~3分**:
- **テスト内並列化**: store パッケージの 94 テストは全て `t.TempDir()` で隔離された
  ストアを使うのに `t.Parallel()` が無く逐次実行だった。全テストに `t.Parallel()`
  を付与し、4コアで **475s → 179s(2.65x)**。
- **CI ジョブ分割**: 単一ジョブの逐次(fmt/vet/build/test/race/fuzz)を
  lint・test・race・fuzz の**独立並列ジョブ**に分割。壁時計を「合計」から
  「最長ジョブ」に。
- **ファジングのシャーディング**: 18 ターゲット×10s の逐次(180s)を matrix で
  3シャード並列(最長 ~80s)に。

**並行バグの発見(t.Parallel 化で露見)**: store を初めて `-race` で並列実行したら
`github.com/jotfs/fastcdc-go` の `NewChunker` で **46 件のデータ競合**が出た。原因は
同ライブラリが**パッケージ大域の gear テーブルを `table[i] ^= Seed` でその場書き換え**
する実装で、複数アップロードを並行チャンク化するとこの大域への書き込みと
`nextChunk` の読み出しが競合する。**これはテスト専用ではなく本番の並行アップロード
経路(concurrent Put)にも存在する実バグ**(Seed=0 なので値は変わらず無害だが、Go
メモリモデル上は未定義動作)。

**修正**: FastCDC のコア(~90行 + 256エントリ gear テーブル)を `internal/chunker`
に自前移植し、**テーブルを読み取り専用**にして競合を根絶。置き換え前後で
**チャンク境界がバイト単位で完全一致**することを実測で確認(random/text/反復/
全ゼロ/端サイズ × 平均サイズ3種)してから `fastcdc-go` 依存を削除。境界は golden
ハッシュ回帰テストで固定し、`TestConcurrentChunkingRaceFree` を `-race` CI に追加。

**教訓**: テストの並列化は速いだけでなく、**本番の並行経路に潜む競合を炙り出す**。
「並行に叩いて -race」は無料の並行性検証になる。未保守の依存が大域状態をその場
書き換えする例は珍しくないので、ホットパスの依存は競合特性を疑うべき。

**続き(全ワークフロー最適化)**:
- **release.yml のサニティ高速化**: リリースビルドのゲートが全テスト(~4分)を
  再実行していた。重い E2E は push ごとの CI が担保済みなので、`go test -short`
  (重い E2E を `testing.Short()` でスキップ、~40s)+ CGO 有無の両ビルドに変更。
  **サニティ ~4分 → ~1分**。
- **ci.yml の test ジョブをシャーディング**: `-run`/`-skip` を同一正規表現で
  与えると**完全な相補分割**になる(Go 仕様)ことを使い、重い E2E
  (Region/Optimize/…)と残り全部の2シャードに分割して並列実行。test ジョブの
  律速 ~235s → ~130s。
- **-short ガード**: 重い store E2E 10 本を `if testing.Short() { t.Skip }` で
  ガード。`-short` 実測 43s(全 157s)。CI の全テストジョブは -short なしで
  全網羅を維持。

## 4.33 独自研究(2026-07-19): H.264 CAVLC 動画の可逆再圧縮 — 「動画は縮まない」の境界を実装で確定

「動画も大きく圧縮する手法」への回答。まず境界を実測で確定した:

**閉じている扉(§4.23 の再確認)**: H.264 High(CABAC)・H.265・VP9・AV1 は
既に**算術符号**でエントロピー限界近くまで詰まっており、可逆では縮まない。
スマホ動画はほぼ全て CABAC。これはどのツールでも同じ(情報理論)。

**開いている扉(本研究)**: H.264 には2つのエントロピーモードがあり、
**CAVLC(ハフマン系 VLC)は Baseline プロファイルの必須モード**。監視カメラ・
webcam・会議録画・ドローン・古い機器のハードウェアエンコーダは低遅延優先で
CAVLC を使う——**ストレージを大量に食う現役ニッチ**。ここには JPEG(§4.21)と
同じ「VLC → 文脈算術」の再圧縮余地がある。実測: 同一映像を x264 で
CAVLC/CABAC 両方にエンコードした差(=理論的な回収床)は **2〜9.1%**
(低ビットレートほど大)。

**実装(純Go・約1400行)**: スライスデータの CAVLC を**構文要素へ解析**し、
文脈適応算術符号(rangecoder)で再符号化する。決定的な逆変換で CAVLC を
ビット単位に再生成。核心は3つ:
1. **ピクセル復号が不要**: IDCT・動き補償・デブロッキング・参照管理は一切
   実装しない。必要な状態は nC 導出用の「4x4 ブロックごとの非ゼロ係数数
   マップ」だけ。実装面積が桁で縮む(JPEG で確立したエントロピー層限定の原理)。
2. **1本の走査コードが両方向を駆動**: 各構文要素サイトは t.ue()/t.se()/… を
   呼ぶだけで、capture(ビット→算術)と rebuild(算術→ビット)を同じコードが
   処理する。方向ごとの複製がないので、非対称バグが構造的に起きない。
3. **VLC テーブルは機械生成**: coeff_token / total_zeros / run_before の表は
   FFmpeg ソースから**スクリプトで自動転記**(h264tables_gen.go)。手写しの
   転記ミスが2度実バグになった(FLC の対応式・cat2 表)教訓から、人手を排除。

**対応範囲**: Annex B・Constrained Baseline 系(CAVLC・プログレッシブ・
I/P スライス・4:2:0・FMO なし)。CABAC・フィールド・I_PCM・SP/SI・High 系は
解析段階で検出して**安全に素通し**。採用前に全体を復元してバイト一致検証+
読み出し時 SHA-256(全 precomp 共通の安全弁)。

**実測(x264 産 CAVLC、ビット一致復元込み)**:

| ストリーム | サイズ | 再圧縮後 | 削減 |
|---|---:|---:|---:|
| 320x240 baseline crf26 | 53,849 | 49,974 | **-7.2%** |
| 同・ノイズ付き crf28 | 50,792 | 48,325 | **-4.9%** |
| QCIF veryslow crf22 | 28,244 | 25,907 | **-8.3%** |
| 640x480 crf23(814KB) | 814,459 | 763,304 | **-6.3%** |

CABAC 床(2〜9.1%)にほぼ届いている。速度: encode 1.7-1.8 MB/s(自己検証込み)、
decode 3.7-4.2 MB/s(読み出しはリージョンキャッシュが吸収)。

**バグ史(記録価値)**: (1) nC≥8 の FLC を「t1=3 だけ別式」と誤解 →
実際は v=4*(tc-1)+t1 が全 t1 共通で、あり得ない組 (tc=1,t1=3) の v=3 を
tc=0 の番兵に転用する設計だった。(2) cat2/total_zeros 表の手転記ミス。
どちらも「実ストリームの前方一致が途中まで成立してしまう」ため、
数ブロック先で顕在化する遅発性デシンクとして現れた。→ 表の機械生成と
ビット単位の突き合わせデバッグ(実ビット列 vs 独立 Python 実装)で解決。

**今後**: (a) MP4/MKV/TS コンテナからの H.264 サンプル抽出(現状は生
Annex B のみ)、(b) レベルの文脈を係数位置で分ける(CABAC 超えの余地)、
(c) MPEG-2/MPEG-4 ASP(DVD・放送アーカイブ、同じ VLC→算術構図で
10-20% 見込み)、(d) B スライス対応(Main プロファイル CAVLC)。

## 4.34 独自研究(2026-07-19): スマホ動画への拡張 — MP4/CAVLC 実装 と CABAC の定量境界

「スマホ動画も縮めたい」への回答。2つの攻め口を実装・測定した。

### (a) MP4 コンテナ対応(実装・採用)— ドラレコ/DVR/旧機/画面録画に即効

§4.33 の CAVLC エンジンをコンテナ非依存化(h264engine.go)し、**MP4(ISO BMFF)
入りの CAVLC 動画**を扱えるようにした。多くのドライブレコーダ・DVR・古い
スマホ・画面録画アプリは低遅延優先で **Baseline/CAVLC を MP4 に格納**している。

- moov のサンプル表(stsd/stsc/stsz/stco/co64・avcC の SPS/PPS)を解析して
  各ビデオサンプルのファイル位置を求め、長さ前置 NAL を CAVLC エンジンで
  トランスコード。**moov・音声トラック(AAC 等)・その他バイトはスケルトンとして
  原文保存**し、算術ストリーム+ヘッダ+原文を1本の Chunked に連結(store の
  重複排除・圧縮も効く)。
- fragmented MP4(moof)・複数ビデオトラック・CABAC は解析段階で検出して素通し。
- 採用前に**ファイル全体を復元してバイト一致検証**。fuzz(FuzzTryUnwrapMP4H264)。
- 実測: CAVLC動画+AAC音声の MP4 で **end-to-end 物理 -5.1%**(音声・コンテナは
  不動、動画トラックだけが縮む)。

### (b) CABAC(現代スマホ動画)の定量境界 — 実装せず、測定で判定

スマホ動画の大半は **H.264 High/CABAC**。CABAC は既に**文脈適応二値算術符号**
であり、可逆再圧縮の余地は「2003年設計の推定器の最適性ギャップ」しかない。
これを実測で確定した:

| ストリーム | 汎用圧縮(zlib-9)での変化 |
|---|---:|
| CABAC(cabac.h264) | **-0.0%**(むしろ +196B 膨張) |
| CABAC(noisy crf26) | **-0.0%** |
| CAVLC(同一映像) | +0.6%(zlib)/ **-6.3%(我々の専用文脈算術)** |

**CABAC 出力には汎用圧縮の効く残余冗長がほぼ無い**=算術符号として密。
CAVLC が VLC ゆえ再算術化で -6% 取れるのとは対照的。

**では CABAC は完全にゼロか?** 完全な 0 ではない。packMP4/研究レベルの
CABAC 再圧縮は **~1〜3%** を報告する。源泉は (1) CABAC の状態遷移が
64状態の粗い量子化で理論確率から離れる、(2) コンテキスト初期化・
バイパスビンの非効率。ただしこの回収には**CABAC 復号器のフル実装**
(~460 コンテキスト・全スライス構文の算術復号、数千行)が必要で、
CAVLC(VLC 表の引き直し)とは実装規模が桁違い。得られる 1〜3% に対して
実装・検証コストが見合わないため、**現時点では実装しない**(FFmpeg から
確率テーブル ff_h264_cabac_tables は抽出済み=着手可能な状態で保留)。

**結論(正直な地図)**:
- H.264 **CAVLC**(Annex B / MP4): **-5〜8%** 可逆 — 実装済み・採用。
  ドラレコ・DVR・監視・会議・旧機・画面録画の実データに効く。
- H.264 **CABAC**・H.265・VP9・AV1: 汎用手段では残余ほぼ 0。専用 CABAC
  再推定で理論上 ~1〜3% だが実装規模が過大。**フル CABAC 復号器を独立
  セッションで実装する**のが唯一の前進路(§4.34b にテーブル準備済み)。
- 可逆で「大きく」縮む動画は存在しない(H.264 以降は設計上そう作られている)。
  ここは情報理論の壁であり、ashuku は縮む所(CAVLC・MJPEG・重複排除)を
  最大化しつつ、縮まない所を正直に素通しする。

## 4.35 独自研究(2026-07-19): CABAC フル復号器の実装と**バイト厳密**検証 — §4.34b の保留を突破

§4.34b で「フル CABAC 復号器は実装規模が過大」として保留した部分を、
**実装し・実ストリームでバイト厳密に検証した**。これはスマホ動画(H.264
High/CABAC)可逆再圧縮の中核基盤であり、最難関(=文脈選択が構文依存で、
1ビンの文脈誤りが以降を総崩れさせる)を越えた。

### 実装したもの
- **CABAC 算術エンジン**(`cabacengine.go`): ITU-T H.264 9.3 の復号器
  (9.3.3.2)と符号化器(9.3.4)の両方。符号化器は規格の正規手続きなので、
  同じビン列・文脈列でビット単位に同一出力を生成する=可逆再圧縮の土台。
  自己整合を `TestCabacIdentity`(200試行のランダムビン列)で確認。
- **文脈テーブル**(`cabactables_gen.go`): FFmpeg cabac.c / h264_cabac.c から
  機械生成(LPS レンジ・状態遷移・全コンテキスト初期化 m,n・有意マップ走査)。
- **I スライス構文復号器**(`cabacmb.go` / `cabacresidual.go`): mb_type(I_NxN/
  I_16x16/**I_PCM**)、イントラ予測モード、intra_chroma_pred、coded_block_pattern、
  mb_qp_delta、残差(cbf→有意マップ→レベル)を FFmpeg と同一の文脈導出で走査。
  画素復号はせず、cbf/有意マップ文脈に必要な「4x4 ごとの非ゼロ数」と
  「MB ごとの cbp16」だけを近傍追跡する(CAVLC 実装と同じ発想)。

### 検証方法:遅発性デシンク・オラクル + 計装 FFmpeg
CABAC は状態依存なので、文脈導出に1つでも誤りがあれば以降のビンが総崩れになり
**end_of_slice(terminate=1)が末尾に整列しない**。これを検出オラクルにした
(`TestCabacISliceOracle`)。デバッグは FFmpeg を**計装ビルド**(各 MB /
各残差ブロックの `range`/`low`/`bytestream`/coeff_count を出力)して**規格の
真値**と突き合わせる方式で行い、以下を厳密に特定:
- 文脈初期化・LPS レンジ・状態表・engine ロジックは全て真値一致(問題なし)。
- **真のバグ**: CABAC 走査器が近傍非ゼロ追跡器(`h264NZ`)の `sliceID`/
  `curSlice` を更新しておらず、`lumaAt` が常に「利用不可」を返していた。
  結果、luma AC の cbf 文脈が常に最大値(base+3)に固定され、MB 内隣接
  ブロックの文脈が誤っていた(MB0 のブロック3で判明:ctx 96 → 正 93)。
  → 走査器が MB を処理する際に nz スライス識別子を歩調合わせて修正。

### 結果(バイト厳密)
実 x264 CABAC 全イントラ QCIF ストリームの **10 スライス・計 990 マクロブロック**
が全て end_of_slice にビット単位で整列(残余は rbsp_trailing の 0〜6 bit のみ)。
I_PCM マクロブロック(生画素 384B + 算術復号器再初期化)も含めて完全走査。

### 再圧縮の実効(実装・測定・往復検証)
バイト厳密な走査器を土台に、二次二値レンジコーダ(`cabacrecomp.go`、
LZMA 系 11bit 適応ビットモデル)へビン列を載せ替えた:
- **capture**: 原 CABAC を復号しつつ各ビンを二次符号へ(文脈コード化ビンは
  ctxIdx 別モデル、バイパスは等確率)。モデルは**スライス跨ぎに持続**し、
  CABAC の per-slice 初期化オーバヘッドを回収。
- **rebuild**: 二次符号から復号→ CABAC へ再符号化(`cabacEncoder`)。

**測定**(実 x264 CABAC 全イントラ、10スライス、`TestCabacRecompMeasure`):
原 CABAC 21692B → 二次 21268B = **-1.95%**(可逆)。§4.34b の推定 1〜3% と一致。

**可逆性の証明**(`TestCabacRoundTrip`): capture→rebuild で原 CABAC 算術本体を
**バイト厳密**に再生。差は末尾フラッシュ(rbsp_trailing のエンコーダ差)の
**1バイトのみ**で、これは recipe に verbatim 保存すれば完全一致=完全可逆。

### 位置づけと次段
- **達成**: I スライス CABAC を規格準拠・バイト厳密に走査し、可逆再圧縮
  (-1.95%、本体バイト厳密+末尾 tail 保存で完全可逆)を実装・検証。
  「文脈を lockstep で再現する」最難関を越え、再圧縮の実効まで到達。
- **残**: P/B スライス(mb_skip・動きベクトル・参照index・sub_mb —
  スマホ動画の大半)。I スライスのみでは全イントラ CABAC(画面録画・
  一部高品質撮影)に直接効く。往復バイト厳密検証で保護するため、
  非対応構文が出れば安全に素通しへ退避する。
- **正直な地図の更新**: 「CABAC は実装規模が過大で保留」(§4.34b)は
  **I スライスについては解消**。得られる -2% は小さいが、CAVLC(-6%)と
  合わせ「動画は縮まない」の壁を規格実装で押し広げた実測値である。

## 4.36 続・研究開発(2026-07-19): CABAC 本番配線と、プログレッシブ JPEG の2つの実バグ修正

### (a) CABAC 再圧縮を本番パイプラインへ配線(動画)
§4.35 の CABAC I スライス再圧縮を capture/rebuild エンジン
(`h264engine.go`)に組み込み、**Annex B と MP4 の両コンテナ**で
entropy_coding_mode スライスが自動的に二次算術へ載るようにした
(P/B スライス入りは従来どおり素通し)。設計上の要点:
- **正準再符号化の並走**: capture 時に復号と同時に正準 CABAC 再符号化を
  走らせ、原文との共通接頭辞長を計算。エンコーダ依存の末尾フラッシュ差
  (通常1バイト)を `[delta][tail]` としてヘッダブロブ末尾へ埋め込む。
  レシピのスキーマ変更なしで任意エンコーダ出力を完全可逆化。
- **I_PCM 完全対応**: 正準エンコーダに flush→バイト整列→生画素→再初期化
  (9.3.4.1)を実装し、x264 実出力とバイト一致することを確認。
- **Annex B レシピ v2**: スケルトン(SPS/PPS/SEI)を JSON(base64 で 4/3 倍)
  から Chunked 先頭のブロブへ移動(MP4 と同じ設計)。旧 v1 レシピも
  引き続き復元可能。SPS/PPS の繰り返しが store の zstd で潰れるようになり、
  CAVLC でも採用判定が正確化。
- **二次モデルの2レート混合**: 速い(>>4)/遅い(>>7)適応カウンタの和
  (12bit)で符号化。単一レート(11bit, >>5)比で全ストリーム改善。
- 実測(採用・往復バイト一致検証つき): 全イントラ CABAC で Annex B
  -1.3〜-2.2%、MP4 -1.3%。CAVLC は従来通り -3.3〜-5.7%。

### (b) プログレッシブ JPEG: 現代エンコーダ出力で発火する2つの実バグ(画像)
現行 PIL 12.x(libjpeg-turbo 3系)でプログレッシブ JPEG を生成して
本番経路に通すと**全滅(採用率 0%)**することを発見。ビット一致検証が
弾いていた=可逆性は守られていたが、利得を全く取れていなかった。
計装した libjpeg-turbo とのシンボル単位突き合わせで2つの根本原因を特定:

1. **補正ビットバッファの溢れフラッシュ欠落**: libjpeg は
   `BE > MAX_CORR_BITS(1000) - 64 + 1` で EOBRUN を強制発行するが、
   うちは `EOBRUN == 0x7FFF` しか見ていなかった。補正ビット密度の高い
   実写真で発火。
2. **ZRL 折り込み判定の位置**: libjpeg は(補正ビットだけの既存有意係数を
   含む)**すべての有意係数の直前**で `run>15 && k<=EOB` の ZRL 発行を
   判定する。うちは新規有意(v==1)のときだけ判定していたため、
   run>15 の途中に既存有意が挟まると補正ビットの分配がデコーダと
   非同期になり、ビット列が食い違っていた。

修正後、**56/56 の掃引**(4ソース × 品質 60/85/95 × サブサンプリング
444/420 × optimize 有無 + RSTマーカ + グレースケール)で全て採用:
実写真 -16〜-33%、スクリーンショット -63〜-76%、ノイズ -5〜-14%。
旧 testdata が通っていたのは低解像度・低補正密度で両バグを踏まなかった
だけで、**実運用のプログレッシブ写真(mozjpeg/PIL の既定)は大半が
素通しになっていた**可能性が高い。修正の実利は大きい。

## 4.37 独自研究(2026-07-19): CABAC P/B スライス+8x8 変換 — スマホ実機動画の可逆再圧縮を達成

§4.35-4.36 の I スライス CABAC を、**スマホカメラ実機の構成そのもの**
(High プロファイル: P/B スライス・8x8 変換・weighted pred・複数参照)へ
拡張した。「スマホ動画の可逆再圧縮」の本丸。

### 実装(cabacinter.go / cabacmb.go 拡張)
- **スライスヘッダ**: B(direct_spatial_mv_pred、L1、両リスト modification)、
  pred_weight_table(P weightp / B explicit)、High 系 PPS tail
  (transform_8x8_mode・scaling matrix・second_chroma_qp)、SPS
  direct_8x8_inference。
- **inter 予測層**: mb_skip_flag(P:11-13/B:24-26)、P/B mb_type ビンツリー
  (P:14-17、B:27-32、intra escape 17/32)、sub_mb_type(P:21-23/B:36-39)、
  ref_idx(54-59、近傍 ref>0 と B の direct 除外)、mvd(40-/47-、近傍
  |mvd| 和の閾値文脈+EG3 バイパス)。FFmpeg と同一の scan8 キャッシュ
  設計で、近傍(ref/|mvd|/direct/skip/8x8DCT)を per-MB 追跡する。
  動きベクトル値・direct 予測は構文に影響しないため一切計算しない。
- **8x8 変換**: transform_size_8x8_flag(399-401)、cat5 残差(64係数、
  8x8 有意マップ写像、4:2:0 では cbf なし)、B_Direct の
  direct_8x8_inference ゲート。
- **inter 特有の文脈規約**: cbf/cbp の「利用不可近傍」既定値が
  intra(64/0x7CF)と inter(0/0x00F)で異なる非対称性を実装。

### 検証(遅発性デシンク・オラクル+計装 FFmpeg)
x264 生成の5ストリーム(P-only Main / weighted P / P+8x8 High /
B+8x8 High / zerolatency 画面録画風)+全イントラ2本、**計 210 スライス
全てが end_of_slice にビット単位で整列**。開発中に計装 FFmpeg
(per-MB range / per-ref / per-mvd トレース)との突き合わせで特定した
実バグ: (1) mvd 保存の Z 順/ラスタ順不一致、(2) SPS direct_8x8_inference
未解析、(3) subDirect フラグの前スライス残留。

### 実測(本番経路、採用+往復バイト厳密検証つき)
| ストリーム | 構成 | 削減 |
|---|---|---:|
| v_cabac.h264 | P 動画(Main) | **-2.7%** |
| v_cabac.mp4 | 同 MP4 | **-3.0%** |
| p_main.h264 | P-only 30f | **-3.4%** |
| b_high.h264 | B+8x8 High | **-3.8%** |
| phone_like.mp4 | 640x360 High B 8x8(スマホ様) | **-2.1%** |

P/B は I-only(-1.3〜-2.2%)より縮む(skip/mvd/cbp=0 系の文脈が
二次モデルの持続適応と相性が良い)。CAVLC(-3〜-6%)と合わせ、
**H.264 のほぼ全実機構成が可逆再圧縮の対象**になった。非対応構文
(インターレース・MBAFF・4:2:2 等)は従来どおり素通しで安全。

## 5. 再現方法

```sh
go run ./cmd/ashuku-bench                     # 全データセット × 全設定
go run ./cmd/ashuku-bench -gens 100 -only 局所変更   # 世代スケーリング
go run ./cmd/ashuku-bench -input yourdata.tar # 実データ
```
