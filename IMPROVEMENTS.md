# 改善課題の全リスト(2026-07-17 全面監査)

**更新(同日)**: P0-1、P1-2〜8、P5-24 を実装済み(下記の ✅)。

コードベース全体(store / api / client / cmd / deploy)を再監査して見つかった、
残る改善点の完全なリスト。優先度順。P0 は実証済みのバグ、P1 は実サービス運用で
確実に問題になるもの、P2 以降はスケール・圧縮・堅牢化の順。

## P0 — バグ(実証済み、即修正すべき)

### 1. ✅ precomp ファイル × クライアント経路ダウンロードの不整合(修正済み)
precompression されたファイル(encoding = gzip-zlib-v1 等)を `ashuku-cli get`
(クライアント支援経路 GET /manifests + GET /chunks)で取得すると、**元の
ストリームではなく展開後のデータがエラーなしで返る**。チャンク列は展開データで
あり、再構成レシピの適用はサーバー経路(GET /files/{id})にしかないため。

実証: 1,222 バイトの gzip をサーバー経路でアップロード → サーバー経路 DL は
一致、CLI DL は 480,000 バイト(展開データ)が返った。サイレントな不一致。

修正方針(いずれか):
- クライアントが `encoding != ""` を検出したらサーバー経路 DL に自動フォール
  バック(最小修正。クライアントに zlib 再構成を実装しない = 技術も漏らさない)
- または handleManifestGet が encoding 付きマニフェストへのチャンク経路を
  409 で拒否し、サーバー経路を案内する(古いクライアント対策として併用が理想)

## P1 — 実サービスで確実に問題になる

### 2. ✅ 全認証ユーザーが管理操作を実行できる(修正済み: @admin キー)
POST /optimize, /scrub, /fsck は認証さえ通れば誰でも実行できる。scrub は
全チャンクの読み出し+伸長+ハッシュ化(TBクラスで数時間のディスク飽和)、
optimize も全走査を伴うため、**一般ユーザーが繰り返し叩くだけでサービスを
麻痺させられる**。管理者キー(keys ファイルに admin フラグ)を導入し、
管理エンドポイントを制限すべき。

### 3. ✅ APIキー = 所有者ID(修正済み: id= でユーザーID分離)
所有者IDとしてAPIキーそのものを使っているため、**キーを変更すると別人になり
既存ファイルへのアクセスを失う**。漏洩時にキーを差し替えられないのは運用上の
重大な欠陥。keys ファイルを「ユーザーID + キー」形式にし、所有者IDには
ユーザーIDを使う(既存データはユーザーID=旧キーとして移行)。

### 4. ✅ 同時リクエストの総メモリ上限がない(修正済み: -max-concurrent、既定512)
チャンクPUTは1リクエスト最大 ~4MiB、precomp は 64MiB×2 をメモリに持つが、
**同時リクエスト数自体に上限がない**(Go の HTTP サーバーは無制限に受ける)。
1000 並行チャンクPUT ≈ 4GB。グローバルなセマフォ(-max-concurrent 系フラグ)
での入場制限が必要。

### 5. ✅ precomp ダウンロードのメモリ無制限(修正済み: precompSem で同時実行制限)
GET /files/{id} の precomp 再構成はファイル全体をメモリで組み立てる
(上限 64MiB×2/件)。並行数の制限がなく、多数の並行ダウンロードでメモリが
膨らむ。アップロード側と同様のセマフォを設ける。

### 6. ✅ Optimize・取り込み途中のディスク残量検査(修正済み: store.MinFreeBytes)
ディスクガードはアップロード開始時のみ。リージョン化・repack・パック
コンパクションは一時的に新旧表現が併存するため書き込みを増やすが、
FreeBytes を確認しない。**残量僅少時に Optimize が自ら満杯を招きうる**。
Optimize 冒頭と各フェーズで -min-free を検査して中断すべき。
また巨大アップロードは開始時に残量があっても途中で埋め切る可能性がある
(putStream 中の定期残量チェックで 507 中断を追加)。

### 7. ✅ シャットダウンで定期ジョブが停止しない(修正済み: context+WaitGroup)
runPeriodic のゴルーチンは context を持たず、HTTP Shutdown 後も走り続ける。
main が Close した後のストアを掴んで実行され、エラー(最悪 DB クローズ中の
競合)になる。context キャンセル+完了待ちに直す。

### 8. ✅ ファイル名長の上限がない(修正済み: 255バイト)
X-File-Name / name は path.Base のみで長さ無制限。巨大な名前がマニフェストと
一覧索引レコードにそのまま入る。255 バイト程度で切る/拒否する。

## P2 — スケールで問題になる(数百万チャンク〜)

### 9. ✅ Optimize の毎時フルスキャン(実装済み: 作業キュー駆動のインクリメンタルパス)
collectStars / offlineDeltaPass / buildRegions / buildSmallChunkRegions /
compactRegions・Packs はすべて全チャンクメタ or 全ファイル走査。1000万
チャンクではメタ読みだけで毎時数GBになる。「前回以降に追加・削除された
チャンク」のダーティリスト駆動(取り込み時に pending キューへ積む)に
再設計する。

### 10. ✅ Stats のフルスキャン(実装済み: 維持カウンタで O(1)、fsck が照合・修復)
Stats() は全ファイル+全チャンク+全パック+全リージョン走査。API 側 TTL
キャッシュ(10s)で頻度は抑えているが、大規模ストアでは1回が秒単位になる。
主要カウンタ(logical/unique/physical/count)を settings バケットで
トランザクショナルに維持し、フルスキャンは fsck の照合用に降格する。

### 11. ✅ Scrub が直列・無制限・キャッシュ汚染(実装済み: ローリング+キャッシュ迂回検証)
全チャンクを1本のループで読む(数TBで数時間)、IOスロットルがなく本番
トラフィックと帯域を奪い合い、readChunk 経由のためチャンクキャッシュを
全チャンクで洗い流す。改善: (1) 進捗カーソル付きローリングスクラブ
(毎日 1/N ずつ)、(2) レート制限、(3) スクラブ読みはキャッシュに入れない。

### 12. ✅ Delete の2トランザクション窓(実装済み: 単一Tx化)
マニフェスト削除(Tx1)と参照解放(Tx2)の間でクラッシュすると、チャンク
参照カウントが実際より高いまま残る(容量リーク。fsck --repair で回収可能)。
releaseChunksTx を Tx1 に統合して原子化できる(削除は頻度が低いので
大きなTxでも許容)。

### 13. bbolt 単一ライタの天井
実測で並行小ファイル ~360件/s(2.7ms/件)。これ以上はメタDBのシャーディング
(ハッシュ接頭辞で複数 bbolt)か別エンジンが必要。単一ノードの現設計では
当面十分だが、限界値として明記しておく。

### 14. 単一ノード(HA なし)
メタバックアップは RPO=1h・同一ボリューム既定。マルチノード複製・
シャーディング(チャンクはコンテンツアドレスなので分散は素直)は最大の
未着手アーキテクチャ課題。

## P3 — 圧縮のさらなる向上(研究ロードマップ)

### 15. preflate 完全統合
現在の precomp は「zlib 産」の deflate のみ(パラメータ探索方式)。preflate
系(任意の deflate 生成物を差分付きで可逆再圧縮)を統合すれば、zlib 以外の
エンコーダ産 gzip/zip にも適用範囲が広がる。外部コードのビルド許可が必要。

### 16. ✅ コンテナ形式の分解(実装済み: ZIP / PDF、zlib 産ストリーム)
ZIP/DOCX/XLSX/JAR/APK(deflate メンバー)、PDF(FlateDecode オブジェクト)を
分解して中身を dedup/デルタ/ソリッド圧縮の対象にする。スケルトン+部分適用
方式で、zlib 産でないメンバーが混ざっていても一致分だけ利得が出る。
実測: 手組み zip 25.8x(従来 ~1.0x)、世代違い zip の物理増分 -82%。
残り: zlib 産でない deflate への対応は preflate 統合(#15)待ち。

### 17. ✅ JPEG 可逆再圧縮(実装済み: 純Go・cgo不要、文脈算術符号で lepton 超え)
baseline JPEG の Huffman を復号し量子化DCT係数を **文脈モデル+レンジ算術符号**
で符号化(§4.18・§4.19)。写真は算術・スクショは zstd を自動選択。ビット一致
検証+往復検証+悪化回避ガード。プログレッシブ・動画は対象外。
- §4.18 初版: 実写真 -22〜-27%(lepton -22% 級)、スクショ -74%。
- §4.19 精緻化: マンティッサ文脈化・活性度に左上ブロック追加・**DC 勾配予測
  (JPEG-LS 中央値予測子)**・低周波帯の独立文脈化を実測で採用し、実写真
  **-29〜-34%** へ向上(lepton 明確に超え)。EOB 明示化は実測で悪化のため不採用。
残: 色成分間予測・量子化テーブル連動の帯重み・文脈混合(優先度低)。

### 17b. ✅ 全種別の後段圧縮強化(実装済み: brotli-11 ベストオブ + BCJ、§4.20)
オフライン経路(チャンク再圧縮・リージョン圧縮)の最終段を libzstd-22 /
brotli-11 / BCJ 併用のベストオブに変更。実測で brotli-11 は全5種
(ログ/JSON/CSV/ソース/バイナリ)で zstd-22 を相対 1〜4% 下回り、BCJ は
実行バイナリでさらに -1.5%。採用は厳密に縮む場合のみ+往復検証つき。
純Goのため cgo なしビルドでも利得。xz/LZMA2(純Go)は全敗で不採用。

### 18. 小チャンクリージョンの改善(一部完了: 辞書学習は不採用と判断 §RESEARCH 4.15)
現在は第1スーパーフィーチャのみでソート。全SF集合での類似クラスタリング、
zstd 辞書学習(--train 相当)を型別に行えばさらに数%積める見込み。

### 19. ✅ リージョンの長距離マッチ(実装済み: level22+大窓+LDM、束16)
実データ測定で 束8+19=9.97% → 束16+22=11.35% 改善を確認し採用。
束32(12.26%)は読み出し増幅とのバランスで不採用(RESEARCH.md §4.15)。

## P4 — セキュリティ強化

### 20. ✅ 認証失敗のレート制限(実装済み: IP単位 20回/分 → 429)
定時間比較は導入済みだが、総当たり自体を遅くする仕組み(IP/キー単位の
失敗回数制限・指数バックオフ)がない。

### 21. ✅ /metrics・/healthz の露出(修正済み)
- /metrics は認証あり運用では**管理者キー必須**に変更(-metrics-public で
  従来の無認証公開へ明示的に戻せる)。
- /healthz から free_bytes を削除(無認証エンドポイントからサーバー規模を
  推定されない。状態文字列のみ返す)。
- あわせて: **500 応答から内部エラー詳細を除去**(実装情報はログのみ)、
  全応答に nosniff / X-Frame-Options / Referrer-Policy ヘッダ、
  **APIキーのハッシュ保存対応**(keys ファイルに sha256:<hex> 形式で置ける
  =ファイル漏洩時にキー自体が漏れない)。

### 22. 重複排除の存在オラクル(既知のトレードオフ)
POST /chunks/missing は「そのチャンクが誰かによって保存済みか」を漏らす
(Harnik らのサイドチャネル)。ダウンロードは ownerchunks で守済みだが、
存在確認自体はクロスオーナー。対策(所有者スコープの dedup)は削減率を
下げるため、脅威モデルと合わせて文書化して選択制にするのが現実的。

### 23. ✅ precomp パーサのファジング(実装済み: 5ターゲット+CI組み込み。gzip FEXTRA のパニックを発見・修正)
gzip/zlib/PNG のヘッダ・レシピ解析は攻撃者制御の入力を扱う。go-fuzz /
testing.F でのファジングを CI に組み込み、パニック・メモリ暴走を排除する。

## P5 — 運用・品質

### 24. ✅ CI がない(修正済み: .github/workflows/ci.yml)
リポジトリに GitHub Actions がなく、build/test/vet/race が push で回らない。
最優先の品質基盤(CGO 有無の両ビルド + race + gofmt チェック)。

### 25. ✅ バックアップの自動検証と保存先分離(実装済み: 検証付き書き出し・-meta-backup-dir・復旧手順を README に)
メタバックアップは書きっぱなしで、開けるかの検証がない(bbolt として
オープン+バケット存在確認を追加)。保存先も data/ 配下固定なので
-meta-backup-dir で別ボリュームを指定可能にする。復旧手順の runbook も未整備。

### 26. ✅ メトリクスの拡充(実装済み: 所要時間ヒストグラム・キャッシュヒット率・グループコミット合流率・認証失敗・throttle)
リクエスト所要時間ヒストグラム、キャッシュヒット率、グループコミット
合流率、Optimize/Scrub の所要時間・進捗、bbolt Tx レイテンシ。

### 27. クラッシュ注入テストの自動化
kill -9 耐久試験は手動で1回行ったのみ。サブプロセスでランダム kill →
再起動 → scrub/fsck 全緑を確認するテストを自動化する。

### 28. ✅ ドキュメント更新(実装済み: README 全面更新+Web コンソール追加)
README に一覧ページング(?limit/?after/next_cursor)、管理エンドポイント、
運用ガイド(scrub/fsck/バックアップ復旧)を追記。アクセスログに所有者と
転送バイトを追加。

---

検証済み事項(このリスト作成時): 全テストスイート緑 / race detector
(並行系テスト)緑 / 書き込みベンチ 順次6.0ms・並行2.7ms・重複0.99ms /
P0-1 のバグは E2E で再現確認済み。
